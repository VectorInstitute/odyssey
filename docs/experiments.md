# Experiment registry

One row per training run. Kept in git because it holds no patient data,
only configs, commits and aggregate numbers. The lead session maintains it
from the operating sessions' reports; report artifacts (JSON, HTML) live in
the gitignored `research_journal/` and on the VMs.

Recipe shorthand: **v6 recipe** = bundle-invariant family-restricted loss,
family balancing alpha 0.5, visit-scoped concepts, icd3 backoff, medication
normalization, time-to-next-event head, per-event hazard heads, randint 0,
64 lanes x 512, 2 epochs, checkpoint_every 2000.

Comparator feature sets: the tuned **GBM** is the strong-feature bar (609
hand-built columns, `odyssey/inference/baseline_features.py`). **TabICL**
rows are fit on the **basic** set (~17 columns) on every dataset: its
in-context forward pass re-reads the whole context at `O(n^2 + n*m^2)` in
context rows and feature columns, so 609 columns costs ~70 GB per
`predict_proba` call versus ~8 GB at 17 (measured 2026-08-23;
`tabicl_baseline.estimate_peak_gb` now refuses such a fit). That is a real
property of the comparator, not a tuning choice, and it keeps the MIMIC and
eICU TabICL rows like-for-like. **EBM** rows are strong-feature on MIMIC and
basic on eICU; **SurvivalPFN** is basic on both. Every row states its own
feature set.

Data versions: **MIMIC** = `mimiciv_3.1_v1` (292/?/37 shards). **eICU v1** =
`eicu_2.0_v1` (spec v1: 36% of medication rows `UNK`, infusions a bare
`INFUSION_DRUG` token; 134/17/17 shards). **eICU v2** = `eicu_2.0_v2` (spec
v2 from bc7ac4c + 60310b0: HICL segment, named infusions, nurseCharting GCS,
intakeOutput urine output; extraction pending the raw tables).

Landmark protocol: `LANDMARK_PROTOCOL_VERSION` (odyssey/inference/alerts.py) stamps every alerts row dump. v1 = per-chunk landmark-mask reset (spurious rows at chunk boundaries); v2 = state carried across chunks (85dde80); v3 = per-lane visit-bucket state, interleaved-visit duplication fixed (dbfd447); v4 (2026-08-30, 059db53) = ONE information boundary at the landmark instant for every scorer: baseline features (basic + strong + notes) now include events AT the index time t, windows (t-w, t], matching the model-side scorers and MEDS-Tab -- under v1-v3 tabular baselines saw one systematically task-relevant event less than the model at every landmark. The same commit fixes label semantics (sepsis3 first-trigger anchored at evidence completion; shock pools cuff+arterial MAP for the recurrence check; SIRS/qSOFA observed-masks require min_criteria measurable components; GCS trigger stamped at the last paired component), and acbc0e2 versions the clinical bin edges on the binner artifact (old checkpoints re-evaluate under the exact edges they trained with -- clinical_edge_version v1 -- so v4 re-eval deltas isolate the label+boundary changes, never train/eval tokenization skew). 7e4d0a0 fixes note-feature leakage: notes index at max(charttime, storetime) availability (radiology storetime lags charttime by a median 2.25h / 22% >6h; discharge ~20h / 41% >24h, measured on the real 2.2 release), so any pre-fix note_embeddings sidecar must be re-embedded before the next strong_text run (the code now refuses it). v4 baseline columns are NOT comparable with v1-v3 rows; sepsis3/shock/sirs/qsofa label-dependent numbers change at 059db53 the same way AKI numbers changed at 3d7ecbb. One-time data audit (2026-08-30, local mimiciv_3.1_v1): validate_meds_dataset(deep=True) clean, and explicit cross-split subject overlap is 0/0/0 across train 291,702 / tuning 36,463 / held_out 36,462 subjects. Rows below predating 2026-08-23 are v1 unless their Key results cell starts with `[protocol vN]`; a protocol re-evaluation is a NEW row for the same run name, never an edit of the old one. Wave runbook: docs/reeval_wave_v2.md.

| Run | VM | Data | Commit | Purpose | Status | Key results |
|---|---|---|---|---|---|---|
| subset_run_v3 | cbm | MIMIC 30 shards | 4a1c0a8 | visit-scoped concepts, pos_weight, on_vasopressors | done | exact top-1 60.7; same-family set 69.9 (med 35.9, diag 31.0, lab 75.2); concepts 0.62-0.99; lever inert |
| subset_run_v4 | cbm | MIMIC 30 shards | 6ac235a+ | RandInt 0.25 A/B vs v3 | done | exact 51.9 (-8.8); set 65.3; lever correctly signed but +0.1 pt only; retired |
| subset_run_v5 | cbm | MIMIC 30 shards | 68ba07c | bundle objective + time head + hazard heads, randint 0 | done | set 74.9 (lab 80.6, med 41.9, proc 37.5, diag 23.4 [loss bug], billing 20.8); time calibrated; alerts hazard 8/12 vs GBM (mean 0.831 vs 0.798) |
| subset_baseline_v5 | cbm | MIMIC 30 shards | 68ba07c | no-bottleneck baseline for v5 | done | set 76.6 (-1.7 vs v5; diag +4.9 for v5); time head equal; alerts hazard 7/12 |
| subset_run_v6 | cbm | MIMIC 30 shards | a883441 | v5 + family-restricted bundle loss (diagnosis fix) | done | 28,775 steps, 3h20m (0.417 s/step); set 75.7 (v5 74.9): diag 41.3 (23.4; v3 31.0), med 43.6 (41.9), proc 38.4 (37.5), lab 81.2 (80.6), billing 26.2 (20.8); concepts 0.61-0.99; time calibration 76.3/92.8/95.8 vs 75.6/92.7/95.5; alerts vs basic GBM: mean AUROC 0.835 vs 0.798, 7/12 (vasopressor and ICU all horizons, death 8h 0.894/0.799; loses AKI by <=0.02, death 24h/72h); banded interventions inert (truth -0.002); report research_journal/12. **Strong tuned GBM (609 features, 2327459): hazard mean AUROC 0.835 vs GBM 0.910, hazard wins 0/12**; closest vasopressor 8h 0.928/0.939, 24h 0.895/0.920, death 8h 0.894/0.911; largest gaps ICU 24h 0.869/0.959, death 24h 0.849/0.941, AKI 8h 0.777/0.897 |
| subset_baseline_v6 | cbm | MIMIC 30 shards | 4298374 | baseline on the fixed loss | done | 28,796 steps, 3h07m; set 77.9 vs v6 75.7 (baseline +2.2): diag 41.25 vs 41.3 (tied; the v5-era 'bottleneck helps diagnoses' was the loss bug), med 47.0/43.6, proc 43.8/38.4, lab 83.2/81.2, billing 31.1/26.2; alerts pending |
| subset_run_v7 | cbm | MIMIC 30 shards | 2327459 | v6 + value_embeddings (numeric value channel A/B) | done | 3h41m; set 76.8 (v6 75.7): lab 82.5 (+1.3), visit +1.7, billing +1.2, proc +0.9, diag 42.0 (+0.7), med 43.5 (-0.1); concepts mean AUROC 0.797 -> 0.807 (tachy/brady/lactate +2); time head unchanged; alerts vs strong GBM 1/12: vasopressor up at all horizons (8h 0.943 beats GBM 0.939), AKI flat (0.764/0.739/0.681), death DOWN 4-6 pts (0.853/0.799/0.768); report journal 14 |
| subset_run_v6_60 | cbm | MIMIC 60 shards | 2327459 | learning-curve point: v6 recipe on 2x data | done, budget-limited | early stop at 31,750 steps (~1.1 epochs on 60 shards vs 2 on 30), 3h38m: set 75.8 (75.7): diag 37.9 (-3.5), med 44.6 (+1.0), proc 39.6 (+1.3), lab 81.2 (flat), billing 27.5 (+1.4); concepts 0.803 (+0.6); alerts vs strong GBM 1/12: death up +2-3 pts (8h 0.914 beats GBM 0.904), vasopressor +1.5-1.8, AKI flat, ICU -0.2 to -2.2. Not a clean scale answer: needs a fixed step budget / larger patience; report journal 17 |
| subset_run_v8 | cbm | MIMIC 30 shards | 8c0a475 | v7 + event_head_hidden 256 + static facts as leading inputs | done | 3h28m; set 76.7 (v7 76.8, flat): proc 40.4 (+1.2), visit 35.2 (+1.5), billing 25.8 (-1.5), diag 41.3 (-0.8); concepts mean 0.796 (v7 0.807; vital-threshold concepts -1 to -4, flag); time head unchanged; alerts vs strong GBM 2/12 (vasopressor 8h 0.949/0.941, 24h 0.924/0.923); death +1.4/+2.9/+3.2 vs v7 (MLP head), AKI flat, ICU -0.4 to -1.8; report journal 18 |
| full_run_v8 | cbm | MIMIC all 292 train shards | c902eea (stream_shards) | first full-scale MIMIC run, v8 recipe, 1 epoch, early stopping off | done | 137,605 steps, 16h41m; learning curve 74.6/76.5/78.5 at 14k/28k/56k; final set top-1 80.4 (subset v8 76.7, +3.7; billing +20.3, proc +10.4, med +7.2); concepts mean AUROC 0.857 (+0.061, every concept up); time NLL 0.256, same-instant 96.1%; banded interventions wrong-signed at full scale (none 36.39, truth 35.86, flip 36.31: truth-flip -0.46 pt; zero_known 5.29, zero_unknown 36.08; all 6 modes); alerts vs strong GBM 2/12, death gap closes +0.09-0.10 (8h flips to a win), AKI +0.02-0.03, ICU +0.03-0.06, vasopressor flat; report research_journal 20 |
| full_run_v8 (N2 time-head probe) | cbm | MIMIC held-out 4 shards (banked 2.89M positions, rate 0.3); fit on 10 train shards (2.0M, rate 0.1), tuning 2 shards (0.98M) | 6d7b0a1 | Track B item 13: alternative time heads on the frozen flagship features (odyssey/inference/time_head_probe.py) | done | bin NLL / same-instant acc / after-bundle-1h pred vs obs: hazard refit 0.24999 / 0.9627 / 0.7557 vs 0.7577; hazard_mlp 0.24936 / 0.9628 / 0.7518 vs 0.7577; categorical 0.25160 / 0.9624 / 0.7561 vs 0.7577; lognormal3 0.26888 / 0.9618 / 0.7347 vs 0.7577 (continuous NLL 0.1286, median-gap abs error 64.2 h). Decision rule (beat hazard on BOTH NLL and after-1h calibration): none graduates; the binned hazard head stands. 24 min wall; JSON ~/runs/full_run_v8/time_head_probe.json |
| full_run_v8 (N3 counterfactual forecasts) | cbm | MIMIC held-out 2 shards, 300 subjects at 24h into a visit | 6d7b0a1 | Track B item 12: input-level what-ifs (odyssey/inference/counterfactual.py; edit the record, re-bin, re-score at the same position) | done | hypotension_6h (SBP set 80 for 6h; 44 edited): vasopressor +0.33 to +0.45 pp, death +0.02 to +0.11 pp, ICU admission +4.7 to +5.4 pp, sign agreement 93-98%; creatinine +1 mg/dL over 24h (246 edited): AKI +0.08 to +0.94 pp, sign agreement 93-94%, concept aki_stage_3 +0.038; lactate x3 (40 edited): death +0.12 to +0.96 pp, vasopressor +0.16 to +0.30 pp, sign agreement 85-90%; normotension (SBP set 120): near-zero deltas, sign agreement ~52% (weak-edit null); remove labs 24h (277 edited): AKI +0.4 to +0.8 pp, tachycardia concept -0.043. Reading: input-level edits move hazards in the clinically expected direction with 85-98% sign agreement, unlike the inert concept-level lever; 8 min wall; JSON ~/runs/full_run_v8/counterfactual.json |
| full_run_v8 (M2 comparator rescore) | cbm | MIMIC held-out 4 shards (552,756 rows, protocol v3); baselines fit on the 30 train shards | 910732d | MIMIC comparator table: hazard vs tuned GBM vs TabICL vs EBM on the same v3 rows | done | [protocol v3] AUROC hazard / GBM / TabICL(basic) / EBM(strong), n at risk (n positive): AKI 8h 0.831/0.893/0.775/0.793 n=95,471(1,505), 24h 0.773/0.845/0.753/0.702 n=84,147(4,019), 72h 0.725/0.783/0.670/0.654 n=57,442(9,580); death 8h 0.962/0.949/0.925/0.921 n=136,850(222), 24h 0.934/0.962/0.902/0.861 n=135,061(682), 72h 0.904/0.936/0.864/0.850 n=130,818(1,949); ICU admission 8h 0.934/0.969/0.858/0.945 n=85,838(1,603), 24h 0.893/0.954/0.801/0.919 n=74,839(2,569), 72h 0.837/0.932/0.722/0.887 n=48,971(3,779); vasopressor 8h 0.938/0.932/0.840/0.834 n=111,450(467), 24h 0.892/0.915/0.793/0.803 n=98,722(974), 72h 0.828/0.886/0.687/0.775 n=67,385(1,801). Hazard beats the tuned GBM on 2/12 (death 8h, vasopressor 8h); the nearest remaining misses are vasopressor 24h (0.023) and death 24h (0.028), and the gap widens with horizon on 3 of 4 events (ICU 0.036 -> 0.095, vasopressor 0.005 win -> 0.058 loss, death 0.013 win -> 0.033 loss) while AKI stays flat at 0.058-0.072; TabICL loses 12/12; EBM loses except ICU admission, where it is close to the GBM and clearly ahead of the hazard heads. **SurvivalPFN (capped refit at 2913d15, done 2026-08-24; fit in-context on a train-split subsample, disjoint from the scored rows, same discipline as the other comparators):** AUROC, with `p_unique` = the number of DISTINCT predicted probabilities over the queried rows, which must be read alongside every value here. AKI 0.6074 / 0.6134 / 0.5504 (p_unique 76,134 / 76,291 / 55,611); ICU admission 0.6380 / 0.6236 / 0.6088 (69,865 / 63,636 / 47,308); vasopressor 0.5223 / 0.5373 / 0.5022 (**175 / 242** / 11,507); death **WITHHELD** / **WITHHELD** / 0.7691 (**1 / 1** / -). Loses to hazard and GBM on every cell it can be scored on. TWO GRADES OF DEGENERACY, and only the first was caught by the withholding rule: (a) death 8h/24h have `p_unique = 1`, every probability identically 0 because the fitted survival curve does not move at those horizons, so `1 - S(h)` is zero by construction; these are withheld and must never be reported as 0.5. (b) vasopressor 8h/24h are NOT constant but are resolution-limited: 175 and 242 distinct values across 111,450 and 98,722 rows, at magnitudes ~1e-4, which is the same near-saturation the `predict_proba` docstring already documented on 2026-08-23. Their 0.5223 and 0.5373 are close to chance and are substantially tie-breaking among a handful of levels, so they are reported here but should NOT be quoted as a measurement of SurvivalPFN's discrimination. The `p_unique == 1` withholding criterion is therefore necessary but not sufficient; a graded flag is the right fix and is not yet implemented. By contrast AKI and ICU admission have tens of thousands of distinct values and are genuine, if weak, measurements. Context sparsity is the leading explanation: the death fit held ~53 events in ~4,857 rows (1.1%), while eICU's death cells under the same code path are informative at a 13.3% context event rate (8h 0.7958, 24h 0.7603). Whether a denser context rescues the short-horizon cells is UNRESOLVED -- the direct test produced a degenerate 149-subject all-dying context and was invalid (research journal entry 50). 1h02m, peak RSS 10.2 GB |
| full_run_v8 (text-modality probe) | cbm | MIMIC held-out 4 shards, 552,756 rows; GBM fit on the 30 train shards | 2005a51 | Track A item 7 probe gate: do pooled note embeddings move the tuned GBM's alerts? Two encoders (BAAI/bge-base-en-v1.5 and google/medgemma-4b-it), 246,302 MIMIC-IV notes, mean-pooled, PCA to 64 dims, added as note count/staleness/mean/last features (`--baseline-features strong_text`) | done | AUROC delta vs the strong-only GBM (8h/24h/72h). BGE: vasopressor +0.003/+0.009/+0.011, ICU +0.001/+0.002/+0.001, AKI -0.003/+0.001/-0.002, death -0.015/+0.000/-0.000. MedGemma: vasopressor +0.006/+0.007/+0.009, ICU +0.001/0.000/0.000, AKI -0.003/+0.004/-0.001, death -0.044/+0.006/+0.004. GATE NOT MET: both encoders agree that text does not move the alerts on this slice -- the only consistent movement is vasopressor (+0.3 to +1.1 pp), ICU and AKI are inside noise, and death@8h DROPS in both arms (-1.5 pp BGE, -4.4 pp MedGemma). The two-arm design was chosen to separate 'text carries no signal' from 'the encoder is weak'; a clinical-domain model does no better than a general one, so the weak-encoder explanation is not supported. Hypothesis, untested: death@8h has the fewest positives of any cell (222 of 136,850), so adding ~131 dense columns hurts it by variance -- consistent with the drop being largest for the larger embedding. Per the roadmap's own rule, fusion work does not proceed on this evidence. Caveats: one slice, one seed, no confidence intervals; pooled whole-note embeddings only (no section or finding-level structure); radiology dominates the corpus (213,285 of 246,302 notes) and discharge summaries are only visible to a later visit. BGE 1h46m / MedGemma 2h37m to embed; each pass 1h46m |
| subset_run_v12_valuehead | cbm | MIMIC 30 shards | 47ac590 | value-head A/B arm B: v8 recipe + `value_head` (9-quantile, code-conditioned, pinball) -- does making magnitude a DISTRIBUTIONAL output close the GBM gap? | done | **[BASELINE MISMATCH, found 2026-08-24 -- the deltas below are NOT VALID as published]** This row's own absolute AUROCs are protocol v3 and stand; but every "vs subset_run_v8" DELTA here was computed against v8's stale v1-protocol dump (1,116,108 rows, no `landmark_protocol_version` column), because no v3 v8 dump existed on disk until 2026-08-24. Confirmed directly: v8's AKI@8h recomputed from that v1 dump is 0.769988, i.e. exactly the 0.7700 quoted here. Arm-to-arm comparisons among B, C, D and E remain valid (all are v3); only the v8 baseline is mismatched. Fix is a rescore of v8's existing checkpoint, not a retrain. Recompute before quoting any delta from this row. 28,873 steps, 3h44m, best val 2.6662; value loss 0.32 -> 0.10-0.12. Hazard AUROC vs subset_run_v8: AKI 8h 0.7826 (+0.0126), 24h 0.7334 (+0.0016), 72h 0.6859 (+0.0145); vasopressor 8h 0.9058 (-0.0436), 24h 0.8612 (-0.0627), 72h 0.8048 (-0.0762); **ICU 8h 0.8986 (-0.0216), 24h 0.8448 (-0.0182), 72h 0.7718 (-0.0085); death 8h 0.9060 (+0.0396), 24h 0.8786 (+0.0511), 72h 0.8607 (+0.0609)** -- the largest single move in the arm, larger than AKI's own predicted-direction shift, on an event the value channel has no obvious mechanistic reason to touch. Set top-1 76.91 (+0.17), lab 82.52 (+0.14). Time head: nll 0.2759 (v8 0.2772), same-instant flat, after-bundle 1h |delta| 0.0098 vs v8's 0.0002 (worse at 1h only). Value metrics (new): CRPS 0.2136, median abs err 0.2737 over 32.97M positions; creatinine CRPS 0.3843, median abs err 0.5023 (0.50 SD) over 430,069; coverage at nominal 0.1..0.9 = 0.140/0.169/0.243/0.393/0.516/0.592/0.709/0.818/0.909 -- badly under-covered through the middle. VERDICT: does NOT close the gap. AKI moves in the predicted direction but by 0.1-1.5 pp against an ~11 pp deficit, while vasopressor loses 4.4-7.6 pp -- a shared-backbone capacity tradeoff of the same shape v9's recency channel showed. Death's +4 to +6pp move and ICU's -0.9 to -2.2pp move were omitted from the original readout; both are read against the GBM's own cross-arm noise floor below (~0.5-1.5pp typical, up to ~2.9pp on the sparsest cell), so death's move is real signal, not noise. CAVEAT that limits the conclusion: the head is a single linear layer (0.01M params) and its own calibration is poor, so this tests a LINEAR, badly-calibrated value head, not distributional value prediction in general; a head that predicts magnitude poorly cannot force useful magnitude retention. |
| subset_run_v8 (value-tail clip measurement) | cbm | MIMIC 1 train shard, 2,409,997 events / 920,291 valued observations with per-code stats (1,377 codes) | b743cf8 | value-head line, arm E motivation: how often does the model's `+-5` input clip actually saturate, and where does the threshold land clinically? Read-only pass over subset_run_v8's own fitted quantile_binner.json, no refit | done | Pooled clipped fraction 1.47% (high 1.30%, low 0.17%). Per code, `z=+5` in raw units / clipped-high share / p99 / p99.9: creatinine (LAB//RESULT//50912//mg/dL, n=11,973, median 0.900, scale 0.445) **3.124 mg/dL / 7.17% / 9.0 / 15.6**; lactate (50813, n=1,883, median 1.700, scale 0.964) **6.518 mmol/L / 3.61% / 9.0 / 13.1**; wbc 1.36%; temperature_f 0.45%; spo2 0.55% low-side (ceiling at 100, expected); heart rate 0.012%, SBP ~0-0.03%, resp rate 0.04%. Top clipped-count code is creatinine (859), then SUBJECT_WEIGHT_AT_INFUSION (750) and several undecoded LAB itemids. DECISIVE READING: creatinine's clip point (3.124 mg/dL) sits BELOW the 4.0 mg/dL threshold the `aki_stage_3` concept is defined on, so a 3.2 and a 15.6 reach the model as the identical input number -- the feature cannot represent the threshold its own label keys on. Lactate has the same shape (6.5 mmol/L clip vs a septic-shock tail to 13). The vital-sign channels do NOT have this problem (<1.5%, mostly past any plausible value), so this is specific to the right-skewed labs, which are the ones behind the AKI and vasopressor deficits. The comparison is also asymmetric: baselines featurize raw unclipped `numeric_value` (odyssey/inference/baseline_features.py), so the tuned GBM reads a tail the model discards. Motivates `value_tail_transform="symlog"` (identity inside `+-5`, `sign(z)*(5+log1p(|z|-5))` outside; ~98.5% of valued observations unmoved) = arm E. CPU-only, GPU untouched (arm C ran at 93% throughout). **Companion NULL, same shard and binner:** a second suspected asymmetry -- codes below `quantile_min_count` (100) get no `value_stats`, so `standardize` returns null, `value_features` reports `has=0`, and the magnitude is invisible while the GBM still featurizes the raw value -- is NOT material: 399 of 920,690 valued observations (0.0433%) have a code absent from `value_stats`. All 399 are also missing a curated-range bin, so they are fully invisible rather than merely un-quantified, but the top no-stats codes are all long-tail (single-digit counts per shard, UNK/N-A units, infusion-end markers), none clinically load-bearing. Mechanism for why coverage is already near-total: `min_count` is evaluated at FIT time over the whole 30-shard train split, not per shard. That also kills a speculative reading of `full_run_v8`'s gap-closing that I had floated -- the floor does NOT bite materially harder at 30-shard than at 292-shard scale, because there is only 0.04% left to recover. Hypothesis dropped |
| full_run_v8 (M-item: MEDS-Tab comparator) | cbm | MIMIC held-out, shared landmark rows (protocol v3); MEDS-Tab tabularized over the same extraction | 358d742 | established-baseline check: is the tuned GBM we measure ourselves against actually a hard bar? MEDS-Tab's own xgboost, hyperparameter sweep capped at `n_trials=10` with boosting rounds capped at 400 | done | JOIN VERIFIED FIRST (the precondition, not an afterthought): every one of our at-risk landmark rows found a MEDS-Tab prediction (`frac_our_at_risk_rows_joined = 1.0`, all 5 cells); MEDS-Tab side 1.00003-1.0001, i.e. a handful of duplicate `(subject_id, prediction_time)` fan-outs, the already-documented duplicate-key phenomenon. AUROC recomputed on the joined rows with OUR `y@h` and at-risk mask matches MEDS-Tab's self-reported `test_auc` to 5-6 decimal places, so the two are not measuring different populations -- which is what the shared-landmark driver was built to guarantee. AUROC hazard / tuned GBM / MEDS-Tab on the SAME rows (n at risk, n positive): death 8h **0.9623** / 0.9490 / 0.9325 (136,850, 222); death 24h 0.9340 / **0.9617** / 0.9024 (135,061, 682); vasopressor_start 24h 0.8922 / **0.9148** / 0.8284 (98,722, 974); icu_admission 24h 0.8933 / **0.9540** / 0.8261 (74,839, 2,569); acute_kidney_injury 24h 0.7727 / **0.8454** / 0.6996 (84,147, 4,019). RESULT: MEDS-Tab is LAST in all 5 cells -- it does not beat the tuned GBM anywhere, and it loses to our own hazard head everywhere too, including AKI 24h (0.6996 vs hazard 0.7727) where the GBM beats both comfortably. The bar we measure ourselves against is not artificially soft. CAVEAT that must travel with this number: MEDS-Tab was deliberately capped at 10 trials / 400 rounds for parity with our GBM's own 4-config / 400-round search (see the DEFAULT_N_TRIALS reasoning in both driver scripts). This shows MEDS-Tab does not win under a FAIR search budget; it is NOT a claim that the method is categorically weaker at unlimited tuning depth. Scope: 5 cells (4 tasks at 24h plus death at 8h), not the full 12-cell comparator grid. Sweep cost 5.5h for the 4-task 24h run; per-trial time varied ~9x across tasks (icu_admission 94s/trial to death_24h 832s/trial). ARTIFACTS PARTIALLY DELETED 2026-08-31 (Amrit instruction, VM1 was at 85% disk with R8 training): the 230GB `meds_tab_out_shared/` tabularization and per-task prediction tree was removed, freeing 229GB (VM1 85% -> 26%). The four result files behind every number in this cell (`meds_tab_auroc_comparison.json`, `meds_tab_sweep_results.json`, `meds_tab_shared_out.json`, `config.json`) were copied to `research_journal/figure_data/vm1/full_run_v8_meds_tab_scoped/` FIRST and verified to contain these exact AUROCs; they had existed only on the VM until then. Retained on VM1: fitted xgboost models (59MB), labels (104MB), the one-shard trust-gate reference (1.4GB), and `alerts_rows.parquet` (34MB). Reproducing this row from scratch now requires a full re-tabularization plus the 5.5h sweep |
| subset_run_v12f | cbm | MIMIC 30 shards | 47ac590 | value-head A/B arm C: v8 + `value_fourier` (input ENCODING of z as sin/cos at 8 geometric frequencies, replacing `[z, z^2, has]`) -- isolates "better input encoding" from arm B's "better output objective" | done | **[BASELINE MISMATCH, found 2026-08-24 -- any "vs v8" delta here is NOT VALID as published]** This row's own absolute AUROCs are protocol v3 and stand; the v8 baseline they were read against was v8's stale v1-protocol dump (1,116,108 rows, no `landmark_protocol_version` column), because no v3 v8 dump existed until 2026-08-24. Arm-to-arm comparisons among B, C, D and E remain valid (all v3); only the v8 baseline is mismatched. Fix is a rescore of v8's existing checkpoint, not a retrain.  28,873 steps, 4h9m, best val 2.6694 -- effectively tied with arm B's 2.6662, the richer encoding bought nothing on validation loss. **CAVEAT that governs the whole row: C ran on the OLD clipped values (tail_transform=clip, unchanged from B/v8), so it tests a richer encoding of a signal that is still tail-saturated at the source (see subset_run_v12_symlog's row for the mechanism). Valid only over the unsaturated ~98.5% of the range; a flat result here does NOT close the encoding question, since encoding may matter once the tail itself is fixed by symlog.** Hazard AUROC vs subset_run_v8: AKI 8h 0.7831 (+0.0131), 24h 0.7309 (-0.0009), 72h 0.6862 (+0.0148); vasopressor 8h 0.9052 (-0.0442), 24h 0.8583 (-0.0656), 72h 0.7958 (-0.0852); ICU 8h 0.9008 (-0.0194), 24h 0.8402 (-0.0228), 72h 0.7661 (-0.0143); death 8h 0.9043 (+0.0379), 24h 0.8776 (+0.0501), 72h 0.8460 (+0.0462). C vs B (not vs v8): every cell within +/-0.9pp of B's own value, sign inconsistent cell to cell -- no systematic Fourier-encoding effect, consistent with the val-loss tie. Value metrics: CRPS 0.2162 (B 0.2136, marginally worse), overall median abs err 0.2766 (B 0.2737, marginally worse); creatinine CRPS 0.3901 (B 0.3843, marginally worse), median abs err 0.5133 (B 0.5023, marginally worse) -- richer encoding did not improve creatinine calibration either, consistent with a saturated target no encoding can recover. Set top-1 76.39 (B 76.91, v8 76.74 -- slightly below both); lab 82.10 (B 82.52, v8 82.38 -- slightly below both). Time head: nll 0.2772 (v8 0.2772, B 0.2759, flat); after-bundle 1h |delta| 0.0153 (worse than both B's 0.0098 and v8's 0.0002), 8h/24h flat and good (|delta| <= 0.0017). VERDICT: clean null over the range it tests. **GBM cross-arm noise floor, computed across C/B/v8's baseline_gbm AUROC on the same 12 (event,horizon) cells: min spread 0.13pp (AKI 8h), max 2.89pp (death 8h, n_pos=222, the sparsest cell), mean 1.02pp.** The GBM's own `random_state` is fixed (seed=0 throughout `_tune_gbm`/`_fit_baseline_grid`) and verified bit-reproducible given identical in-memory data (3 back-to-back fits, 0.0 max abs diff) and shard file discovery is deterministically sorted -- so the spread is not an unseeded-model artifact, but its exact source (most likely tiny floating-point differences from polars' multi-threaded aggregation propagating through the feature-build pipeline across separate process invocations) was not isolated further. Practical consequence: read every hazard-vs-GBM delta in this table against a ~0.5-1.5pp noise floor, ~3pp on sparse cells, not against zero. |
| subset_run_v12_symlog | cbm | MIMIC 30 shards | 3b0a618 | **arm E, and the primary test of the value line**: v8 + `value_tail_transform="symlog"`, ONE change, input-side only (no value_head, no value_fourier). Motivated by the measured clip defect (row above): the model's input value tail saturated below the threshold its own AKI label is defined on, while the baselines featurize raw unclipped values | **done** | HYPOTHESIS: part of the AKI/vasopressor deficit against the tuned GBM is a representational handicap we imposed on ourselves, not an architectural limit. PREDICTION, stated before the run: AKI moves most, and the movement concentrates on rows whose visible lookback contained a would-have-been-clipped value. PRE-REGISTERED READOUT (fixed 2026-08-24, before any result): symlog changes only ~1.5% of valued observations and every other position is bit-identical to v8, so an aggregate AUROC is computed mostly over rows where E and v8 are the same model on the same input -- a real effect can read as flat. Because clipping is patient-correlated (a renal-failure patient has ALL creatinines above the clip, not 1 in 14), the affected stratum is small but enriched for AKI positives. So E reports hazard AUROC for E and v8 split by EXPOSED vs UNEXPOSED, per event at 8h/24h, n and n-positive per cell. EXPOSED = the row's lookback contains an observation with `|z_raw| >= 5` AND still inside the post-truncation window (`_visible_after_truncation`); the truncation correction matters because truncation hits the highest-volume, i.e. sickest, subjects -- the same population whose creatinines clip -- so the naive at-or-before-time mask would mislabel exactly the rows the stratum exists to isolate. Verified, not assumed: static rows never carry a numeric value (0 of 999 on the sampled shard), so the timed-row truncation criterion is exact for this mask. DECISION RULE (fixed in advance): improves on EXPOSED and flat on UNEXPOSED -> mechanism confirmed, aggregate size is just the exposure rate. Flat on BOTH -> hypothesis wrong, register as such. Moves UNEXPOSED materially -> implementation bug, NOT a finding; stop and report before writing anything up. Also pre-registered: report all four events, not just AKI -- movement on an event with no plausible mechanism is a warning sign, not a bonus win. **PRE-REGISTRATION DEVIATION, made before any arm E result was seen (2026-08-24):** the pre-registered EXPOSED rule was "any observation with `|z_raw| >= 5`" over any code in `value_stats`. Validated on real held-out AKI rows before running the readout: that rule measures **84.4% exposure**, dominated by sentinel values (`numeric_value = 999999.0`) and near-zero-scale codes (`scale = 4.5e-05`), not clinical extremes -- **78 events across 12 subjects** when restricted to curated clinical-range codes (creatinine among them), which is the mechanism the arm actually tests. The primary stratum is therefore narrowed to the curated-code allowlist (`exposure_mask(..., code_allowlist=...)`, b7fdd76); the any-code and not-curated (artifact-only) strata are still reported alongside it, not dropped. The timing is the point: narrowing a stratum before results is a methods decision, the same change after results would not be. **Consequence for the arm itself, not just the mask:** under clip every one of those artifacts saturated to exactly 5 and was indistinguishable from a genuine extreme; under symlog they spread out and reach `SYMLOG_CEILING`, so their `z^2` input feature quadruples. symlog therefore amplifies sentinel and degenerate-scale values at a higher row rate than the creatinine mechanism it was built for. If E's effect appears only on the artifact-only stratum, that is noise amplification and not a win. A flag-gated `min_scale` floor exists (b5e50ea) but is defaulted OFF so E and D differ in exactly one respect, and it does not rescue sentinels in any case | **RESULT (2026-08-24), full detail research_journal/experiments/51_arm_e_stratified_readout.html (local-only):** rescored subset_run_v8's existing checkpoint to v3 protocol first (its own dump was stale v1, 1,116,108 rows vs E's 552,756; row keys verified exact match after, both directions). AKI -- the event this hypothesis was built around -- is a CLEAN NULL at every horizon, both strata: 8h EXPOSED -0.0048/UNEXPOSED -0.0129, 24h +0.0020/+0.0035, 72h +0.0039/+0.0010, all within GBM noise-floor scale, CIs fully overlapping. Symlog did not do what was predicted on the event it was designed for. DEATH triggers the pre-registered stop condition: UNEXPOSED moves +0.096/+0.079/+0.066 at 8h/24h/72h -- 60-90x the GBM's own v8-vs-E spread on the identical cells (+0.002/-0.001/-0.001) -- and EXPOSED moves by nearly the same amount at every horizon, so the two strata aren't behaving differently, which argues against a value-tail mechanism rather than for one. Not written up as a symlog effect per the decision rule. LEADING HYPOTHESIS for the death confound: E is the first arm trained on VM2 (odyssey-eicu-a100); v8/B/C are all VM1 (odyssey-cbm-a100) -- E differs by machine as well as config, and B/C (VM1 vs VM1) show no such pattern, which argues for a cross-machine effect over ordinary seed variance. Untested. RECOMMENDED NEXT STEP, not run: retrain v8's own config on VM2, compare to v8-on-VM1 -- a cheap, decisive test that also bears on L1-L4 (prior VM2-vs-VM1 MIMIC comparisons). Beyond arm E: death's UNEXPOSED move is a methodology-level finding on its own -- two "comparable" training runs differing by 60-90x the GBM's refit noise floor for reasons unrelated to what's being tested means every existing cross-run hazard delta in this registry (including arm B's vasopressor move above) sits inside a band this wide; the strongest case yet for `bootstrap_auroc` (merged 2026-08-24, `odyssey/inference/uncertainty.py`) needing a wired-in home. ICU admission shows the textbook EXPOSED-only shape (+0.014/+0.025/+0.029 EXPOSED, ~flat UNEXPOSED) but on an event with no stated creatinine-clip mechanism and overlapping EXPOSED CIs -- the pre-registered warning sign, not a second result. Vasopressor 24h/72h move ~-0.02 on both strata, plausibly the same confound at smaller magnitude, not decisive. Exposure rates measured: curated(primary) 29.86%, any-code 85.39%, artifact-only 85.19% |
| subset_run_v8_vm2 | cbm | MIMIC 30 shards | 5fe2d50 | **cross-VM control, launched directly from arm E's confound (row above).** v8's own config, byte-identical to `subset_run_v8`'s (diffed key-for-key against `subset_run_v12_symlog`'s config with only `value_tail_transform` removed -- confirmed the only difference), trained on VM2 (`odyssey-eicu-a100`, the machine E trained on) instead of VM1 (`odyssey-cbm-a100`, where `subset_run_v8`/B/C were trained). **HYPOTHESIS, stated before any result:** if `subset_run_v8_vm2` differs from `subset_run_v8` (VM1) by roughly the magnitude arm E differed from `subset_run_v8` on death's UNEXPOSED stratum (+0.065 to +0.096 AUROC, 60-90x the GBM's own cross-arm noise floor), the machine explains arm E's death confound, and every prior VM2-vs-VM1 comparison this program has made needs revisiting, including L1-L4. If the two v8 runs agree to within the GBM's own noise floor (~0.5-1.5pp typical, ~3pp on sparse cells), the machine hypothesis is disconfirmed and the weaker ordinary-training-variance hypothesis (entry 51) becomes the leading explanation instead. **DECISION RULE:** score both against the same v3-protocol held-out shards, same event/horizon cells as arm E's readout; a hazard AUROC delta on the SAME (unexposed-by-construction, since neither run has ever seen symlog) rows that lands within the GBM noise floor confirms the machine is not the driver; a delta of similar magnitude to E's confirms it is | **launched 2026-08-24, training** | Prerequisite for interpreting arm D and everything after it -- assigned by the lead directly after arm E's readout surfaced the confound, ahead of the M3/M4/M6/N1 queue (whose own gates are separately under audit, see below) |
| subset_run_v8 (GBM feature-group ablation) | cbm | MIMIC held-out 4 shards, baselines fit on 30 train shards (v3 protocol) | 5fe2d50 | Error analysis requested directly by Amrit after arm E's null: what does the GBM have that the hazard head doesn't, on AKI/vasopressor/icu_admission at all 3 horizons. 609 strong features partitioned into 5 groups (recency 64, latest_value 48, summary_stats 384, counts_occurrence 110, static 3), drop-one + keep-only, hyperparameters held fixed at the already-tuned values (caveat: this inflates measured closure -- every % below is an upper bound) | done | AKI: summary_stats leads drop-one at every horizon (20.5%/24.5%/22.2% margin closure) and is strong in both directions (keep-only alone: 0.86/0.83/0.76 vs full 0.89/0.85/0.78), but no group collapses the gap -- 75-80% survives every ablation. Vasopressor/ICU: counts_occurrence explains 70-173% of the margin (>100% cells: ablated GBM falls below the hazard head) and alone recovers 94-99% of the full model at every one of 6 cells -- checked for outcome-code leakage (drug.vasopressor.n_6h/n_24h/ever_visit share vasopressor_start's exact onset regex; corrective re-fit removing just those 3 features showed a noise-level effect, 0.9322->0.9364/0.9118->0.9099/0.8895->0.8911; ICU's family.visit checked by code composition, ICU_ADMISSION//MICU at 227 occurrences vs thousands of routine urine-output/transfer codes, not re-fit since the check was already conclusive). Part B (descriptive disagreement, no new fits): ICU shows 0/0/0 haz_right_gbm_wrong under two binned threshold definitions (top-quartile/below-median and top-half/bottom-half), but 22-25% under an unbinned any-margin test -- reported as "no confident-vs-confident disagreement" not "zero complementary information," the stronger claim is not supported. AKI's GBM wins concentrate on short-history/busy-visit patients; vasopressor's on stale-creatinine rows (17-23h since last vs 8-10h) -- an integrated hypothesis (not confirmed): the hazard head degrades when local recent signal is sparse, while explicit counts/aggregates stay well-defined regardless, which motivates explicit windowed counts as the next arm with a pre-registered stratum (sparse-history/stale-lab rows) rather than an aggregate hope. Full detail research_journal/experiments/52_gbm_error_analysis.html (local-only) |
| subset_run_v12_mlp | cbm | MIMIC 30 shards | 3b0a618 | arm D: v8 + symlog + `value_head` + `value_head_hidden=256` + `value_head_weight=2.0`. A clean SUPERSET of arm E, so E is D's control and the pair separates "fixing the input tail helps" from "fixing the tail AND predicting it distributionally helps" | **done** | Revised from the original clip-based config after finding that the value head's own regression target is the same clipped `numeric_z`: under clip, ~7% of creatinine TARGETS are the literal constant 5.0, which is the most likely mechanical cause of arm B's worst-calibrated code (median abs err 0.50 SD) and means a clip-based D would have repeated a known-broken target. Report value metrics against arm B's (CRPS 0.2136, median abs err 0.2737, creatinine CRPS 0.3843 / 0.5023, coverage 0.140/0.169/0.243/0.393/0.516/0.592/0.709/0.818/0.909 at nominal 0.1..0.9) and the head parameter count. If coverage moves toward nominal and creatinine error drops well below 0.50 SD, the saturated-target explanation is confirmed whether or not the alert numbers move -- a result worth having either way. Same EXPOSED/UNEXPOSED stratification as E | **RESULT (2026-08-24):** 28,873 steps, 3h55m (14107.3s), best val **2.7689** -- against B 2.6662 and C 2.6694, same machine (VM1): the MLP head (0.20M params, hidden=256, weight=2.0; vs B's 0.01M single linear layer) cost ~0.10 validation loss over both simpler heads. **VALUE METRICS CONFIRM ARM B'S CAVEAT, cleanly:** creatinine CRPS 0.3165 (B 0.3843) and median abs err 0.4106 SD (B 0.5023 SD) -- an 18% relative improvement, the largest move in any value metric across the whole line. B's poor creatinine calibration really was the saturated-clip-target-plus-one-layer-head combination; fixing the target (symlog) and the head (MLP) fixes the calibration. Overall CRPS 0.2096 (B 0.2136), median abs err 0.2690 (B 0.2737) -- smaller aggregate gains, expected since only ~1.5% of values were ever saturated. Coverage at nominal 0.1..0.9: 0.099/0.172/0.286/0.347/0.485/0.543/0.672/0.774/0.889 (B: 0.140/0.169/0.243/0.393/0.516/0.592/0.709/0.818/0.909) -- mixed, still badly under-covered through the middle on both; better creatinine error did not translate into better aggregate coverage. Set top-1 76.74 (B 76.91, v8 76.74, ties v8); lab 82.50 (B 82.52, ties). Time nll 0.2777 (v8 0.2772, B 0.2759); calibration_after_bundle |delta| 1h 0.0261 (worse than v8's 0.0002 and B's 0.0098), 8h/24h fine (<=0.0047). **So the alert numbers still do not move on AKI (below) even with a properly-sized distributional value head trained against an unsaturated target -- better magnitude prediction does not translate into better AKI discrimination. This closes the value-modelling line more decisively than arm E alone: the saturated-target hypothesis for B's calibration is confirmed, and separately, fixing calibration does not fix discrimination.** HAZARD AUROC vs v8(v3), same-machine (VM1), the clean cross-run comparison: AKI 8h 0.7796 (v8 0.7850, -0.0054), 24h 0.7320 (+0.0054), 72h 0.6881 (+0.0080); vasopressor 8h 0.9022 (-0.0101), 24h 0.8505 (-0.0206), 72h 0.7890 (-0.0167); ICU 8h 0.9009 (+0.0032), 24h 0.8439 (+0.0076), 72h 0.7675 (+0.0183); death 8h 0.9334 (v8 0.8510, **+0.0824**), 24h 0.9054 (**+0.0846**), 72h 0.8786 (**+0.0742**) -- GBM control moves <=1pp on every cell but death 8h (0.97pp, inside the established noise floor), so death's move is in the hazard head itself, not the GBM's known ordering defect (the hazard head is order-invariant). D vs B and D vs C (both VM1, all v3; C isolates the head alone): AKI and ICU flat on both, +/-0.4pp everywhere; vasopressor D 0.3-1.6pp worse than both; death D beats both by 1.8-3.3pp. Since C has neither symlog nor the value head and still shows a large death move vs v8, **the death anomaly is common to B, C AND D alike, not specific to either tested mechanism.** D vs E: cross-machine (D=VM1, E=VM2), reported for completeness only, no conclusion drawn (D val_loss 2.7689 vs E's 2.5546, not otherwise compared). EXPOSED/UNEXPOSED stratification (identical methodology to arm E's pre-registration), D vs v8(v3), CURATED stratum (primary; exposure rates any-code 85.4%, curated 29.9%, artifact-only 85.2%): **AKI does not confirm the mechanism** -- 8h EXPOSED -0.0027/UNEXPOSED -0.0072 (flat-to-negative both strata), 24h EXPOSED +0.0019/UNEXPOSED +0.0076 (UNEXPOSED moves more than EXPOSED, the wrong asymmetry). **Death triggers the pre-registered stop condition a second time**: UNEXPOSED moves +0.1031/+0.0864 at 8h/24h, EXPOSED moves +0.0848/+0.0891 -- both strata move by comparable amounts, exactly arm E's pattern, and at comparable magnitude to E's own UNEXPOSED move (+0.096/+0.079) despite D and v8 being VM1-VM1 matched while E was VM2-VM1. Not written up as a symlog or value-head effect per the decision rule. **This VM1-VM1 recurrence is the decisive evidence against the cross-machine hypothesis floated in arm E's row**: if death's anomaly were a VM1-vs-VM2 artifact it should not appear here at all, since D and v8 never left VM1; `subset_run_v8_vm2` (row above) should be read against this finding when it lands. ICU again shows the textbook EXPOSED-only shape (8h +0.0162 EXPOSED/+0.0000 UNEXPOSED, 24h +0.0314/+0.0008) matching E's own ICU pattern, still not the pre-registered target. Vasopressor: consistent losses, slightly larger on EXPOSED (8h -0.0225 EXPOSED/-0.0067 UNEXPOSED, 24h -0.0269/-0.0201) -- a real negative result, not noise-shaped. **CROSS-MACHINE HYPOTHESIS UPDATE:** with B, C and D all VM1-matched against v8 and all showing the death anomaly, the machine explanation is very likely wrong. Field-by-field `config.json` diff, v8 (8c0a475) vs B/C/D: every differing field (`backbone`, `task_set`, `signal_channels`, `recency_features`, `unfreeze_top_backbone_layers`, `concept_global_pairs`, `freeze_bottleneck`) is a `TrainingConfig` field added strictly AFTER v8's training commit (verified via `git merge-base --is-ancestor` on each introducing commit) -- v8's config.json predates them entirely rather than merely differing in value, and B/C/D hold what read as each field's inert/off default (False/None/0/'v1'). No smoking gun in the config surface itself; if a commit-range explanation is right, the mechanism is more likely something not visible in config.json (e.g. RNG-stream consumption shifting with new-but-inert module instantiation) than an active behavioral toggle |
| eicu_subset_v7 | eicu | eICU v2 30 shards | bdb6ef4 | v6 recipe on spec-v2 data (HICL medications, named infusions, GCS, urine output) | done | 10,483 steps, 1h06m; set 85.3 (flat): lab 87.2, diag 60.4 (58.4), other 62.8 (GCS/urine now 'other'), med 45.2 (45.8; but v1's 31% blank medication events had normalized to a bare MEDICATION//started token, trivially predictable, so v7's flat number with real drug identities is a genuine gain; medication vocab 608 -> 1,260), visit 33.6 (28.6); on_vasopressors AUROC 0.83 -> 0.88, qSOFA 0.85 -> 0.86; time calibration unchanged; alerts vs strong GBM 0/9 (vasopressor 8h 0.870/0.903, AKI 8h 0.711/0.882, death 8h 0.908/0.934); medication audit: novel starts set-hit 16.0 vs repeats 43.1; report journal 15 |
| eicu_subset_v8 | eicu | eICU v2 30 shards | 3d603cf | v7 + value_embeddings + event_head_hidden 256 + static facts as leading inputs | done | 1h08m; set 86.4 (v7 85.3): lab 88.4 (+1.2), proc 46.6 (+1.1), diag 61.1 (+0.7), med 45.6 (flat), visit 29.1 (-4.5, n shifted); concepts mixed (on_vasopressors 0.896, sirs 0.892 up; aki_stage_3 0.923, tachycardia 0.885 down); alerts vs strong GBM 0/9, hazard within +-3 pts of v7 (vasopressor 8h 0.862/0.903, AKI 8h 0.681/0.878, death 8h 0.908/0.943); concept-proxy scorer for vasopressor <0.5 at all horizons (flag); medication audit flat; report journal 16 |
| subset_run_L1 | eicu VM | MIMIC 30 shards | a14c386 | v8 + concept_global_pairs (leakage test) | done | 3h18m; set 75.3 (v8 76.7): med 42.2, diag 37.9, lab 81.0; concepts mean 0.801; banded interventions: none 26.6, truth 24.5, flip 24.2, random 24.3 (truth-flip +0.28 pt, but every intervention below none); alerts 0/12 vs strong GBM |
| subset_run_L2 | eicu VM | MIMIC 30 shards | a14c386 | L1 + randint_prob 1.0 (teacher-forced concept inputs) | done | 3h27m; set 75.2 (L1 75.3): diag 32.7 (-5.1), proc 30.9 (-7.1), billing 16.5 (-6.0), lab 81.1; interventions none 25.9, truth 25.9, flip 25.8, random 25.9 (separation 0.05 pt); zero_known -1.6 (94% retained), zero_unknown -25.6 (1% retained): teacher forcing made the heads MORE dependent on the residual; alerts 0/12 and worse than L1 (ICU 8h 0.814) |
| subset_run_L3 | eicu VM | MIMIC 30 shards | a14c386 | L1 + unknown_dim 8 (residual capped) | done | 3h17m; set 75.0 (flat vs L1); exact top-1 none 24.3 (L1 26.6); interventions truth 19.0, flip 19.5, random 19.3 (all -5 vs none; truth-flip -0.5, wrong-signed); zero_known -20.0 (18% retained), zero_unknown -11.4 (53% retained): load crossed onto the named concepts; concepts mean 0.789; alerts 0/12 |
| subset_run_L4 | eicu VM | MIMIC 30 shards | e879b97 | L1 + unknown_dim 1 (extreme residual cap) | done | 3h17m; set 74.2 (L1 75.3): diag 37.9 (flat), med 40.3 (-1.9), lab 80.0 (-1.0); exact top-1 none 22.3; interventions truth 15.6, flip 15.0, random 15.3 (all -7 vs none; truth-flip +0.55 pt, correctly signed but swamped by the collapse under any edit); zero_known -22.2 (0.5% retained, near-total loss), zero_unknown +0.6 (103% retained, residual carries nothing); concepts mean 0.792 (0.628-0.991); alerts 0/12. Capping the residual to 1 dim doesn't fix the lever -- it makes every edit mode (including truth) catastrophic, because the model has nowhere else to route information when the known channel is disturbed |
| full_run_eicu_v8 | eicu | eICU v2 all 134 train shards | e879b97 | first full-scale eICU run, v8 recipe, 1 epoch, streamed | done | set top-1 87.5 (subset v8 86.4, +1.1); alerts vs strong GBM 0/12 (death 8h 0.940 vs 0.943); interventions: truth/flip/random within 0.4 pt, zero_known 53.7 -> 17.9, zero_unknown unchanged; report research_journal 21 |
| eicu_subset_indep_a | eicu | eICU v2 30 shards | fa1c90c (branch) | independent training stage A: task_weight 0, concept-only backbone | done | 10,488 steps, 69min; feeds entry 25 |
| eicu_subset_indep_b | eicu | eICU v2 30 shards | fa1c90c (branch) | stage B: freeze_bottleneck, init_from stage A, randint 1.0 | done | 53min; truth-flip +0.78 pt (best of series) but truth ties none; residual still dominant; set top-1 collapsed to 47.1; alerts 0/12; report research_journal 25 |
| subset_run_indep_a | cbm | MIMIC 30 shards | fa1c90c (branch) | independent training stage A on MIMIC | done | 28,873 steps, 3h21m; feeds entry 26 |
| subset_run_indep_b | cbm | MIMIC 30 shards | fa1c90c (branch) | stage B on MIMIC | done | banded: none 14.91, truth 14.98, flip 14.46, random 14.74 (truth-flip +0.52 pt, truth beats none, first time); zero_known 18.8% retained (known channel dominant), zero_unknown 94.3%; alerts 0/12, hazard -0.04-0.19 vs v8; report research_journal 26 |
| subset_run_M1 | cbm | MIMIC 30 shards | 850ed2c | resume subset_run_indep_b (entry 26 stage B) from checkpoint_24000.pt, num_epochs 2->5, early_stopping_patience 15->200 | done | 71,697 steps, 3h59m, no early stop; best_val_loss 3.6537->3.6233; set top-1 62.9 (indep_b 62.6); truth-flip +0.57pt (best of series), zero_known 12.0% retained (healthiest completeness yet); alerts 0/12 vs strong GBM, flat to slightly worse than indep_b on most rows; report research_journal 32 |
| subset_run_M2 | cbm | MIMIC 30 shards | 850ed2c | fresh stage B from subset_run_indep_a, unfreeze_top_backbone_layers=2 (of 8), recipe otherwise identical to indep_b | done | 17,750 steps, 1h34m, early stop 15/15; best_val_loss 3.3325; set top-1 65.6; truth-flip +0.35pt; zero_known 27.1% retained; alerts 0/12 vs strong GBM, consistent step up over indep_b and M1 on every row; report research_journal 32 |
| subset_run_M3a | cbm | MIMIC 30 shards | 850ed2c | fresh stage A, task_weight 0.0->0.1 vs subset_run_indep_a | done | 28,873 steps, 3h22m; best_val_loss 2.8883 (task_weight=0.1 scale, not comparable to stage-B numbers); no separate alerts/interventions eval, feeds subset_run_M3b |
| subset_run_M3b | cbm | MIMIC 30 shards | 850ed2c | stage B, init_from subset_run_M3a instead of indep_a, recipe otherwise identical to indep_b | done | 11,000 steps, 0h54m, early stop 15/15; best_val_loss 2.8602 (vs indep_b 3.6537); set top-1 73.5, closes ~77% of the set-top-1 gap to fully-joint v8; truth-flip +0.30pt (weakest of the series); zero_known 25.5% retained; alerts 0/12 vs strong GBM but closest of the series to a GBM win (death@8h 0.901, within the GBM refit noise band 0.908-0.930); report research_journal 32 |
| eicu_tabicl_redo | eicu | eICU v2, eicu_subset_v8 checkpoint_best | branch 8a8145f | entry-27 redo at the checkpoint convention after the default-checkpoint fix (2d61902) | done | TabICL (zero-shot, basic features) wins 5/12: all 3 AKI horizons (0.733/0.710/0.664 vs 0.681/0.672/0.636) and ICU 24h/72h (0.860 vs 0.741 at 72h); hazard wins all vasopressor and death pairs and ICU 8h (matches entry 16 exactly); targeted complementarity, not general superiority; report research_journal 27 |
| eicu_subset_v9 | eicu | eICU v2 30 shards | c5733f0 | v8 + recency_features (per-family hours-since-last, appended after the bottleneck to time/event heads only) | done | 1h27m; alerts hazard +0.008 to +0.022 on 9/12 pairs vs v8 (GBM held fixed as control, flat), AKI 8h 0.681 -> 0.697 (~1/3 of the gap to GBM 0.878); death flat (near ceiling); concept AUROC and time calibration untouched (as designed); forecasting NOT free: set 86.7 (+0.3) but diagnosis -2.7, procedure -1.4, category-set -2.1, offset by demographic +3.7, lab, visit; a real family-level tradeoff via shared-backbone gradients. Contamination measured negligible: a clean v1 rerun (2026-08-22) reproduces to ~1e-7 relative on the metrics checked (death@8h, AKI@8h); numbers above stand unchanged, see research journal entry 38. **`recency_features` REMOVED from the codebase (2026-08-25), decision not a retraction**: a frozen-feature probe on `subset_run_v8` (MIMIC, no recency feed) found the backbone already recovers per-family recency at pooled R^2 0.925 (pre-bottleneck) and 0.916 (post-bottleneck) against ground truth, both from a linear-ish probe with no extra information -- the representation was already substantially there without ever being fed it. Reading: this row's win most likely eased the event-hazard heads' optimization under competing multi-task gradient pressure rather than supplying missing information the model could not otherwise reach. Kept as hand-engineered, GBM-mirroring input plumbing anyway (as was `signal_channels`, the untested `v10` sibling lever, also removed): the project's stated direction is an end-to-end forecaster, not one with per-family staleness handed in. The numbers above stand as history; `load_run` can no longer reconstruct this run's checkpoint (the head-input-width auto-detection this depended on was removed with the feature), so this row is now read-only. |
| eicu_ebm_baseline | eicu | eICU v2, v8 checkpoint_best | d332dbf | NAM/GAM comparator (EBM, basic features, speed-tuned: 100k rows subject-grouped, outer_bags 4) | done | 12/12 fits, 3h28m; loses to hazard AND GBM on every legitimate event (vasopressor 0.699-0.810, AKI 0.575-0.683, death 0.630-0.863); ICU admission outlier (0.961-0.970) confirmed a shared age-dominated artifact of the eICU-degenerate readmission sub-task (excluded per entries 13/21); no uncapped confirmation run |
| eicu_survivalpfn | eicu | eICU v2, v8 checkpoint_best | 2aaa1e2 | Survival-native comparator (SurvivalPFN, basic features, one CPU-only in-context fit per event on (T, delta) targets, scored via a positional re-join against the already-dumped alerts_rows.parquet, zero model forward passes) | done | 9/9 fittable pairs scored (icu_admission skipped: single-class survival delta among >135k at-risk rows, consistent with the known-degenerate eICU event); loses to hazard AND GBM everywhere (vasopressor 0.646-0.681 vs hazard 0.767-0.862; AKI 0.582-0.589 vs 0.636-0.681; death 0.704-0.794 vs 0.787-0.908; GBM ahead of both throughout); duplicate-sensitivity check on the stored dumps (alerts_rows.parquet has 9,108 real duplicate landmark keys, up to 5x multiplicity): GBM robust everywhere, max |delta| 0.0011; hazard's max |delta| 0.0026 (icu_admission@8h) -- stored third-decimal-digit ties should not be over-read; report research_journal 35 |
| eicu_subset_v8 | eicu | eICU v2 30 shards, checkpoint_best | 60d0d45 (dump regen) | [protocol v3] re-evaluation of the v8 alerts row set (E1) | done | [protocol v3] 629,068 rows (stamp confirmed; v9 regen dump matches exactly). Hazard / strong GBM AUROC: AKI 8h 0.699/0.880, 24h 0.681/0.826, 72h 0.642/0.773; vasopressor 8h 0.870/0.908, 24h 0.825/0.886, 72h 0.782/0.853; death 8h 0.895/0.938, 24h 0.857/0.924, 72h 0.786/0.888; ICU admission (degenerate on eICU) 8h 0.780/0.934, 24h 0.756/0.926, 72h 0.722/0.908. Hazard wins 0/12; v1 row above stands as history |
| eicu_survivalpfn | eicu | eICU v2, v8 checkpoint_best | 5d9bd98 (fit script; refit on v3 rows) | [protocol v3] SurvivalPFN refit on the v3 row set (E5) | done | [protocol v3] 629,068 rows, exact key + y@h match to alerts_rows_v3.parquet; 9/9 fittable pairs (icu_admission skipped, same >=50 event+censored floor as v1). SurvivalPFN AUROC: AKI 0.584/0.594/0.591, vasopressor 0.705/0.678/0.670, death 0.796/0.760/0.701 (8h/24h/72h) -- loses to hazard and GBM everywhere, ordering unchanged from v1; RSS 25-28 GB |
| eicu_subset_v6 | eicu | eICU v1 30 shards | 4d8a5b2 | pipeline replication on eICU with the v6 recipe | done | 10,289 steps, 1.02h; set 85.3 (lab 86.8, diag 58.4, med 45.8, proc 46.2, visit 28.6, other 54.2); concepts 0.64-0.96; time calibration within 0.2 pt; alerts hazard vs GBM: vasopressor 8h 0.856/0.838, 24h 0.805/0.800, 72h 0.737/0.763; AKI 0.70/0.74, 0.69/0.71, 0.65/0.68; death 0.878/0.837, 0.825/0.836, 0.756/0.775; ICU admission n/a on eICU (unit stay = visit) |
| gemini_smoke_1 | gemini H200 | GEMINI MEDS 5 train shards | gemini mirror, Aug 22 | first GEMINI training smoke (14M, v8-family recipe, default lanes) | done | 9,266 steps, 26min, best_val 3.63; proves env/feed/checkpointing on the H200 |
| gemini_smoke_2 | gemini H200 | GEMINI MEDS 30 shards | gemini mirror, Aug 22 | 6x data at same 14M params (G1 anchor for the data-scaling curve) | done | 53,575 steps, 2.2h, best_val 2.73; eval-forecast (held-out): set top-1 56.6, labs 58.3, diagnoses 16.0 |
| gemini_full_14m_v1 | gemini H200 | GEMINI MEDS 894 train shards | gemini mirror, Aug 22 | G2: first full-data GEMINI run (14M, 64x512, 2 epochs) | running | ~2.1-3.1 steps/s (feed-bound); val 2.1368 at step 27,200 mid-epoch 0; eval-forecast (G3) after completion |
| gemini_extract_v1_full | gemini | GEMINI SQL -> MEDS | gemini 6281be6 | full 18-table extraction incl. death (derived-flag primary), ICU, ER, transfers, CMG/HIG | done | 1.2007B rows (counts rounded to 1k for export), 1118 shards, 1,118,000 subjects; manifest all-complete; recovery from 2 OOM-kill orphan waves (1,590 footerless files deleted, zero row loss verified vs prior summary); finalize gated on train-full + eval |
| gemini_full_14m_v1 (eval) | gemini H200 | GEMINI MEDS held-out | gemini mirror, Aug 23 | G3: eval-forecast at full data (the third data-scaling point: 5/30/894 shards at fixed 14M) | done | train: 76,868 steps / 10.2h / 2 epochs, best_val 1.9517 AT FINAL STEP (under-trained, supports G4); held-out: set top-1 76.0 (smoke_2 56.6), top1 21.0, top5 47.7, ppl 557 over 20.9M predictions; by family (set top-1): lab 82.9 (smoke_2 58.3), visit 62.9 (top5 88.9), billing 49.3, medication 43.7, procedure 31.1, diagnosis 27.4 (smoke_2 16.0), other 51.7; time head: nll 0.416, same-instant acc 94.9 at 82.5 rate; time calibration predicted-vs-observed within 0.2pt at every horizon (1h .9215/.9222, 8h .9851/.9848, 24h .9971/.9971; after-bundle 1h .5536/.5551, 8h .9155/.9132, 24h .9836/.9832); every family scales strongly with data at fixed params |
| subset_run_v8_taskset_v3 | cbm | MIMIC 30 shards | 9285f68 | **Found run, unregistered until 2026-08-28**: first `task_set="v3"` training run. v8 recipe otherwise unchanged (`value_head`/`value_fourier` off, `value_tail_transform="clip"`, `recency_features`/`signal_channels` off); `task_set="v3"` widens concepts to 26 (v1's 15 + sepsis3 + 11 v3 electrolyte/metabolic/shock concepts) and, via `ALERT_TASK_SETS["v3"] == ALERT_EVENTS_V2`, widens alert events to add sepsis3 + readmission_30d. Needed to unlock the sepsis3/readmission comparator and to feed the bottleneck-signal probe rows below | done (2026-08-25), found and registered 2026-08-28 | 28,873 steps, 4h36m (16583.1s), best_val_loss 2.5830, no early stop. [protocol v3] hazard vs tuned GBM AUROC, n at risk (n positive): vasopressor_start 8h 0.896/0.934 (111,450; 467), 24h 0.833/0.914 (98,722; 974), 72h 0.755/0.883 (67,385; 1,801); icu_admission 8h 0.899/0.968 (85,838; 1,603), 24h 0.843/0.954 (74,839; 2,569), 72h 0.773/0.931 (48,971; 3,779); acute_kidney_injury 8h 0.777/0.894 (95,471; 1,505), 24h 0.726/0.845 (84,147; 4,019), 72h 0.682/0.782 (57,442; 9,580); death 8h 0.904/0.953 (136,850; 222), 24h 0.876/0.959 (135,061; 682), 72h 0.850/0.940 (130,818; 1,949); **sepsis3, first-ever comparator** 8h 0.854/0.937 (89,107; 1,374), 24h 0.834/0.918 (77,651; 2,571), 72h 0.781/0.874 (50,443; 3,662); **readmission_30d, first-ever comparator** 168h 0.504/0.664 (4,769; 452), 720h 0.527/0.723 (4,578; 1,143). Hazard loses all 17 cells to the GBM at this 30-shard scale, consistent with every other subset-scale comparator in this registry (full_run_v8's 292-shard M2 table is the only one where hazard wins any cells). readmission_30d is the standout: hazard is barely above chance (0.50-0.53) against a GBM that is itself weak (0.66-0.72) -- both near floor, but the hazard-to-GBM gap (16-20pp) is the widest of any task in the table, wider than AKI's own well-studied deficit. Near-chance hazard AUROC against a GBM that clearly beats chance points at the hazard head or task formulation for this event specifically, not just "hard task" -- see the CI-check row below. |
| subset_run_v8_taskset_v3 (bottleneck signal probe) | cbm | MIMIC held-out 4 shards, fit on 5 train shards | 9285f68 | **Found run, unregistered until 2026-08-28.** Direct test of whether the concept bottleneck itself discards task-relevant signal: `scripts/probe_bottleneck_signal.py` (built 2026-08-25, run same day, never registered) fits frozen StandardScaler+LogisticRegression probes separately on the backbone hidden state (pre-bottleneck) and the bottleneck output (post-bottleneck, what the event/task heads actually read), per (event, horizon). Precedent it extends: the same method applied to recency-recovery found R^2 0.925 pre / 0.916 post (commit cd96842, the finding that got `recency_features`/`signal_channels` removed as redundant) | done (2026-08-25), found and registered 2026-08-28 | 168,875 train landmark rows / 7,301 visit-end rows; 138,189 held-out landmark / 5,869 visit-end. Pre-bottleneck / post-bottleneck AUROC (delta): vasopressor_start 8h 0.9027/0.8689 (-0.034), 24h 0.8551/0.8388 (-0.016), 72h 0.7866/0.7987 (+0.012); icu_admission 8h 0.9011/0.8928 (-0.008), 24h 0.8434/0.8401 (-0.003), 72h 0.7704/0.7800 (+0.010); acute_kidney_injury 8h 0.8055/0.7988 (-0.007), 24h 0.7519/0.7429 (-0.009), 72h 0.6970/0.6890 (-0.008); death 8h 0.9253/0.8295 (**-0.096**), 24h 0.9149/0.8764 (-0.038), 72h 0.8899/0.8808 (-0.009); sepsis3 8h 0.8732/0.8649 (-0.008), 24h 0.8473/0.8435 (-0.004), 72h 0.7931/0.7904 (-0.003); readmission_30d 168h 0.5964/0.5571 (-0.039), 720h 0.6539/0.5856 (**-0.068**). READING: for vasopressor/ICU/AKI/sepsis3 the bottleneck's own compression cost is small (0.3-3.4pp on the LINEAR-PROBE ceiling) -- the concept bottleneck is not where most of the GBM gap comes from for these tasks. death@8h and readmission@720h lose far more (6.8-9.6pp), the two largest drops in the table by 2-3x. A 1-train/1-held-out-shard smoke run (`probe_smoke.log`, same day) reproduces the same sign and rough magnitude on every cell, so this is not a small-sample artifact. CAVEAT stated in the script's own docstring, and load-bearing: this measures what a frozen LINEAR probe can recover, not what the actual (nonlinear, purpose-built) hazard head extracts -- the CI-check row below shows the two diverge sharply on exactly the two cells that lose the most here (death@8h, readmission@720h), in OPPOSITE directions. |
| subset_run_v8_taskset_v3 (probe vs hazard-head CI check) | cbm | MIMIC held-out 4 shards, joined against `alerts_rows_{v3,readmission_v3}.parquet` | 9285f68 | **Found run, unregistered until 2026-08-28.** Follow-up to the row above: is the post-bottleneck probe's AUROC (a proxy for how much signal is even IN the representation) different from the ACTUAL trained hazard head's own AUROC on the identical held-out rows? `scripts/probe_ci_check.py` (present only on this VM as an untracked file, not yet committed anywhere as of 2026-08-28) reuses the probe's held-out predictions, joins them against the existing alerts dumps by exact row key, and scores both with `odyssey.inference.uncertainty.bootstrap_auroc` (subject-clustered, n_boot=1000, alpha=0.05, the module's own defaults) so the comparison sits on the same finite-sample-variance footing | done (2026-08-25), found and registered 2026-08-28 | probe / hazard AUROC [95% CI], verdict: vasopressor_start 8h 0.869 [.846,.890] / 0.896 [.870,.920] OVERLAP; 24h 0.839 [.815,.861] / 0.833 [.800,.865] OVERLAP; 72h 0.799 [.771,.824] / 0.755 [.713,.797] OVERLAP; icu_admission 8h 0.893 [.880,.905] / 0.899 [.888,.909] OVERLAP; 24h 0.840 [.825,.857] / 0.843 [.827,.860] OVERLAP; 72h 0.780 [.754,.805] / 0.773 [.745,.801] OVERLAP; acute_kidney_injury 8h 0.799 [.781,.817] / 0.777 [.758,.793] OVERLAP; 24h 0.743 [.721,.764] / 0.726 [.701,.750] OVERLAP; 72h 0.689 [.662,.716] / 0.682 [.654,.710] OVERLAP; death 8h 0.829 [.787,.871] / **0.904 [.875,.931], SEPARATED, hazard wins**; 24h 0.876 [.849,.899] / 0.876 [.846,.905] OVERLAP (tied); 72h 0.881 [.859,.900] / 0.850 [.817,.880] OVERLAP; sepsis3 8h 0.865 [.850,.879] / 0.854 [.840,.868] OVERLAP; 24h 0.843 [.827,.859] / 0.834 [.817,.850] OVERLAP; 72h 0.790 [.767,.813] / 0.781 [.756,.805] OVERLAP; readmission_30d 168h 0.557 [.525,.589] / 0.504 [.476,.534] OVERLAP; 720h **0.586 [.565,.608] / 0.527 [.503,.551], SEPARATED, probe wins**. READING, the decision-relevant one: on 15/17 cells the trained hazard head is statistically indistinguishable from a frozen linear probe reading the same post-bottleneck vector -- for AKI/vasopressor/ICU/sepsis3 specifically, the head is already extracting essentially everything a linear probe can find there. Closing the GBM gap on those tasks needs MORE SIGNAL reaching the representation in the first place (consistent with the 2026-08-24 GBM feature-group ablation, whose leading finding was that explicit counts/aggregates -- not encoding richness or head capacity -- drove 70-173% of the vasopressor/ICU margin), not a bigger or better head. The two cells that DO separate go opposite directions: death@8h, the hazard head clearly beats the linear-probe ceiling (0.904 vs 0.829) -- real, separately-valuable nonlinear/censoring-aware work, and representation is not what limits death@8h. readmission_30d@720h is the opposite and the more actionable finding: a linear probe on the SAME frozen representation beats the trained hazard head (0.586 vs 0.527, CIs fully separated) -- the representation already contains more readmission signal than the head is using. That is a head/task-formulation problem, fixable without touching the backbone or the bottleneck, not a representation problem. |
| subset_run_v8_taskset_v3 (EHRSHOT-style probe benchmark, SMOKE SCALE) | cbm | MIMIC 1 train shard, 1 held-out shard | e231fb7 | First run of `scripts/probe_alerts.py` (`odyssey/inference/probe_baseline.py`, built 2026-08-28 per Amrit's redirect: stop adding trained alert heads per task -- documented multi-task loss competition, e.g. eicu_subset_v9's recency channel cost -2 to -9pp on other families -- and instead measure whether the GENERAL representation supports a task under a FROZEN probe, the same evaluation philosophy EHRSHOT itself uses). 5 lab-severity concept tasks (anemia/hyperkalemia/hypoglycemia/hyponatremia/thrombocytopenia, task_set v3, adapted from EHRSHOT's 5-way severity multiclass to binary "concept triggers within horizon h") plus `long_los` (EHRSHOT's length-of-stay >7d, scored from a single ~20-28h snapshot per visit, not a landmark sweep, since it is a static per-visit label). SMOKE SCALE ONLY (1/1 shards, ~33k landmark rows): a functional-correctness run for the new library code (also exercised the `load_run` config-compat fix from the same day), not a decision-grade result -- a 5/4-shard run is the natural next step | done (2026-08-28) | probe_pre / probe_post [95% CI] / gbm [95% CI] AUROC, n at risk (n positive): anemia 8h 0.688/0.634 [.575,.691]/**0.867 [.819,.916]** (25,969; 151), 24h 0.672/0.643 [.587,.704]/0.828 [.774,.879] (22,708; 357), 72h 0.653/0.644 [.575,.703]/0.823 [.766,.877] (14,787; 790); hyperkalemia 8h 0.718/0.618 [.561,.671]/0.782 [.714,.833] (26,978; 150), 24h 0.669/0.601 [.542,.645]/0.804 [.739,.856] (23,737; 339), 72h 0.580/0.578 [.529,.624]/0.747 [.693,.797] (15,819; 709); hypoglycemia 8h 0.679/0.635 [.587,.681]/0.700 [.631,.768] (27,572; 159), 24h 0.580/0.545 [.497,.590]/0.671 [.607,.732] (24,316; 389), 72h 0.544/0.522 [.468,.562]/0.631 [.567,.692] (16,422; 858); hyponatremia 8h 0.694/0.608 [.539,.668]/0.913 [.881,.941] (28,167; 97), 24h 0.640/0.613 [.547,.672]/0.865 [.806,.910] (24,825; 228), 72h 0.627/0.629 [.582,.679]/0.806 [.742,.863] (16,661; 533); thrombocytopenia 8h 0.768/0.681 [.639,.732]/0.957 [.943,.969] (24,850; 198), 24h 0.772/0.688 [.642,.749]/0.945 [.924,.965] (21,688; 450), 72h 0.704/0.653 [.604,.713]/0.907 [.862,.937] (14,052; 793); long_los (snapshot) n/a/0.647 [.615,.685]/**0.705 [.676,.739]** (1,168; 243). READING: **the GBM wins all 16/16 cells, every probe-vs-GBM CI pair fully separated** -- a starker, more uniform gap than the original 4 alert tasks ever showed (subset_run_v8_taskset_v3's own row above has hazard losing 17/17 too, but by smaller margins on several cells, e.g. AKI@8h 11.7pp vs anemia@8h's 23.3pp probe-vs-GBM gap here). SECOND finding, distinct from the GBM comparison and arguably more important for the representation question: **post-bottleneck is lower than pre-bottleneck on every single one of the 15 concept-task cells** (e.g. anemia@8h 0.688->0.634, thrombocytopenia@8h 0.768->0.681), unlike the original 4 v1/v2 alert tasks where pre/post cost was small (0.3-3.4pp, see the bottleneck-signal-probe row above). These 5 concepts ARE part of this run's own task_set=v3 concept-bottleneck supervision (so the bottleneck was trained to predict them specifically) yet still loses substantially more of their *linearly-recoverable* signal than the earlier, unrelated v1/v2 concepts did -- consistent with, though not proof of, capacity competition among the 26 concepts task_set v3 trains the bottleneck on simultaneously. CAVEATS that must travel with this row: smoke scale only (1 shard each side, positives as low as 97-198 on the 8h cells) -- CIs are wide and this is a functional-correctness check, not a claim about the architecture at scale; GBM here is the FULL tuned search (`tune=True`, not the fast `tune=False` path), so it is a fair, not inflated, comparator. **Recommended next step, not yet run:** the same benchmark at 5 train / 4 held-out shards, matching every other probe row's convention in this table, before drawing an architecture conclusion from the pre>post pattern specifically. |
| subset_run_v8_taskset_v3 (EHRSHOT-style probe benchmark, FULL SCALE) | cbm | MIMIC 5 train shards, 4 held-out shards | e231fb7 | Same benchmark as the smoke-scale row above, at the scale every other probe row in this table uses (5/4 shards), run to check whether the smoke run's pre>post-bottleneck gap for the 5 v3-only concepts was real or a small-sample artifact | done (2026-08-28) | probe_pre / probe_post [95% CI] / gbm [95% CI] AUROC, n at risk (n positive): anemia 8h 0.779/0.753 [.729,.775]/**0.911 [.895,.927]** (109,501; 595), 24h 0.753/0.734 [.713,.758]/0.899 [.880,.916] (96,903; 1,526), 72h 0.717/0.707 [.680,.732]/0.877 [.854,.898] (66,044; 3,386); hyperkalemia 8h 0.790/0.735 [.707,.762]/0.817 [.787,.843] (114,502; 587), 24h 0.742/0.706 [.672,.737]/0.814 [.780,.845] (101,896; 1,304), 72h 0.691/0.666 [.634,.698]/0.755 [.719,.790] (70,898; 2,669); hypoglycemia 8h 0.740/0.724 [.702,.746]/0.768 [.743,.792] (115,944; 593), 24h 0.635/0.623 [.602,.648]/0.684 [.652,.715] (103,274; 1,471), 72h 0.615/0.605 [.580,.632]/0.683 [.649,.715] (72,193; 3,258); hyponatremia 8h 0.765/0.716 [.685,.748]/**0.914 [.893,.932]** (114,595; 494), 24h 0.709/0.686 [.657,.714]/0.887 [.863,.911] (101,806; 1,128), 72h 0.662/0.646 [.615,.676]/0.828 [.801,.855] (70,443; 2,415); thrombocytopenia 8h 0.841/0.825 [.800,.848]/**0.968 [.959,.976]** (107,552; 785), 24h 0.814/0.804 [.778,.829]/0.952 [.940,.964] (95,239; 1,743), 72h 0.769/0.762 [.733,.789]/0.918 [.899,.936] (65,141; 3,029); long_los (snapshot) n/a/0.681 [.663,.701]/0.735 [.717,.753] (4,620; 1,090). READING: **GBM still wins all 16/16 cells** at full scale, every CI pair still separated (closest margins: hypoglycemia@8h 0.724 vs 0.768, and long_los 0.681 vs 0.735 -- both real but the smallest gaps in the table; widest: thrombocytopenia@8h 0.825 vs 0.968). **The smoke run's headline pre>post finding was partly a small-sample artifact -- the full-scale drops are 2-4x smaller across the board.** Pre-bottleneck/post-bottleneck deltas at 8h: anemia -0.026 (smoke: -0.054), hyperkalemia -0.055 (smoke: -0.100), hypoglycemia -0.016 (smoke: -0.044), hyponatremia -0.049 (smoke: -0.086), thrombocytopenia -0.016 (smoke: -0.087) -- every single cell's gap shrank substantially with more data, confirming the smoke row's own stated caveat. REVISED READING: anemia/hypoglycemia/thrombocytopenia now lose about as much through the bottleneck (0.7-2.6pp at 8h, shrinking further by 72h) as the ORIGINAL v1/v2 tasks did (0.3-3.4pp, see the bottleneck-signal-probe row above) -- no longer a distinguishable pattern. hyperkalemia and hyponatremia still lose more (4.9-5.5pp at 8h) than the v1/v2 ceiling, a real but much softer signal than the smoke run suggested, and not uniform across all 5 concepts the way the smoke run implied. **Net effect on the capacity-competition hypothesis (smoke row's speculative reading): weakened, not confirmed, and no longer supported as a clean/uniform effect across task_set v3's concepts -- at most 2 of 5 concepts show an elevated bottleneck cost, and even those are a few points, not the 5-10pp the smoke run showed.** The dominant, robust finding at both scales is unchanged: GBM wins every cell, by a wide and now tightly-estimated margin. |

**Retired: context-truncation stratification** (2026-08-24). Not deferred, retired: every run in this comparison (v8, E, D, and every prior arm) trains `backbone="hybrid"`, which never invokes `PackedContextSampler`, so `IndexRow.is_tail` is always False and no truncated stratum exists on any run we report or will report under the current architecture. Confirmed directly rather than assumed: 0 of 33,340 rows on a sampled real AKI shard. `exposure_mask` still implements and tests the truncation correction, because it is meant to outlive this arm. Revisit only if a transformer-backbone arm is run. Note this retires the truncation ANALYSIS only; Track A item 9, the model-scale ladder, is untouched and still open pending a costed decision.

**Open defect: non-deterministic index-row ordering inflates cross-run comparator variance** (found 2026-08-24, fix pending). Symptom: the tuned GBM is a control in the value-tail arms -- it never reads the value channel and scores the same held-out shards in every arm -- yet its own AUROC differs across arms, reading 0.9452 / 0.9518 / 0.9229 on death@8h for C / B / v8, a 2.9pp spread, and 0.5-1.5pp elsewhere.

Diagnosis, empirical and in this order. The GBM classifier is NOT unseeded: `random_state=seed` is threaded through and three in-process fits on identical data are bit-identical (max abs diff 0.0). Column order does NOT vary: `feature_names_hash` is identical across separate processes, so `feature_names()` really is fixed by module constants and the `set(codes)` at `baseline_features.py:418` only builds lookup dicts. What DOES vary is row order: across two invocations of the same script in separate processes, `keys_hash` and `x_full_hash` differ while **`x_nansum` is identical to ten decimal places** -- the same multiset of values, differently ordered.

Root cause: `_index_rows_from_events` (`odyssey/inference/alerts.py:1934`, the default `index_mode="landmark"` path) calls `group_by(...).agg(...)` with no `maintain_order=True` and no subsequent sort, then zips the result positionally. Polars does not guarantee group order without it, and its multi-threaded grouping varies run to run. Siblings at 1861/1906/1915 share the pattern; three other call sites in the same files DO set `maintain_order=True`, so this reads as an oversight.

Causal link to the AUROC swing: `_tune_gbm` (`alerts.py:1137-1176`) subsamples when rows exceed `GBM_TUNE_MAX_ROWS = 200_000` using a seeded rng, so it draws the same POSITIONAL indices every run -- applied to a differently-ordered array, fixed positions select a genuinely different subset of subjects, hence a different fitted GBM. `vasopressor_start` has 990,670 rows at 30 shards, so the path is live in production. This is a real mechanism, not float noise: 1e-15 aggregation differences cannot produce a 2.9pp swing, a different training subsample can.

Blast radius: the hazard head is UNAFFECTED (AUROC is order-invariant and it scores all rows). Every comparator that SUBSAMPLES is exposed to the same mechanism and needs checking: TabICL's row cap, SurvivalPFN's `SURVIVALPFN_MAX_ROWS = 5000`, EBM's 100k subject-grouped sample.

Framing: published comparisons were internally consistent WITHIN each run. What this cost is cross-run precision, so past numbers are imprecise across arms rather than wrong. Fix queued (explicit sort at the grouping sites, plus keying `_tune_gbm`'s subsample off stable identifiers rather than array positions, which removes the class rather than today's symptom). **Acceptance criterion: re-measure the GBM cross-arm spread after the fix; if it does not collapse toward zero the diagnosis is incomplete and nothing is closed.** Note this partly REMOVES rather than merely quantifies the noise floor, so it interacts with the bootstrap-uncertainty work (2501436): that measures finite-sample variance, this shrinks refit variance.

**Naming collision: there are TWO unrelated "M-series"** (found 2026-08-24 by an audit, after the lead issued a "run M3, M4, M6" order that could not be satisfied by either).
1. `docs/experiment_plan.md`'s VM1/MIMIC wave queue: M1 epoch-2 eval chain, M2 wave MIMIC dump, M3 v9-MIMIC case-study regen, M4 L2-L4 intervention reruns, M5 (done), M6 v9-MIMIC seed replicate. That doc is dated 2026-08-23 and is framed entirely around `LANDMARK_PROTOCOL_VERSION=2`, superseded by the v3 work.
2. This registry's OWN `subset_run_M1` / `M2` / `M3a` / `M3b` rows: the stage-B training-recipe experiments (README roadmap item 10). **This series has no M4 and no M6.** N1 (`docs/track_b_designs.md`, "combine the M-series' two partial wins") refers to M2 + M3a from THIS series, not from the plan doc's queue.

Consequences, all verified rather than inferred: M6 as written is gated on eICU E2/E3 and is DEAD under the 2026-08-23 MIMIC-only directive. M3/M4 are gated on "M2 done", meaning a v2-protocol wave dump that has no corresponding row here and has in any case been overtaken by the v3 comparator work landed under different names. N1 itself is real, MIMIC-only, no eICU dependency, ~4-5h A100, but carries the same ambiguous "wave closed" gate.

**Authority ordering, per the 2026-08-23 directive: the README Roadmap section is authoritative.** `docs/experiment_plan.md` and `docs/track_b_designs.md` both need a pass against the README roadmap and against this registry before anyone executes against their gate language again. Do not issue or accept work items by bare M/N identifier without naming which series and checking the gate against this file.

**Found work: `subset_run_v8_taskset_v3` and both bottleneck-signal probes ran 2026-08-25, sat unregistered on the VM until 2026-08-28.** Discovered while looking for prior pre/post-bottleneck probe results: `~/runs/` on `odyssey-cbm-a100` had a full v3-task-set training run plus two completed probe log sets (`probe_smoke.log`, `probe_full2.log`, `probe_ci_check.log`) that were never reported to the registry. Recorded as three rows above; headline synthesis for anyone deciding what to try next toward beating the GBM:
1. For **vasopressor_start / icu_admission / acute_kidney_injury / sepsis3**: the concept bottleneck barely costs signal (0.3-3.4pp on a linear-probe ceiling), and the trained hazard head already extracts essentially all of what a linear probe can find post-bottleneck (15/17 cells statistically overlap in the CI check). The GBM gap on these tasks is upstream of both the bottleneck and the head -- it needs more signal reaching the representation in the first place. This corroborates, from an independent angle, the 2026-08-24 GBM feature-group ablation's finding that explicit counts/aggregates (not encoding or head capacity) drove most of the vasopressor/ICU margin: **explicit windowed count/aggregate features feeding the backbone remains the best-supported next architecture change for these tasks**, not a bigger bottleneck or head.
2. **death@8h** is the one cell where the trained hazard head clearly outperforms the linear-probe ceiling (0.904 vs 0.829, CIs separated) -- the nonlinear, censoring-aware head is doing real work there. Not a target for representation changes.
3. **readmission_30d@720h** is the standout actionable lead: hazard AUROC (0.527) is barely above chance and 20pp behind the GBM (0.723), yet a linear probe on the SAME frozen post-bottleneck representation scores 0.586 (CIs separated from the hazard head) -- the representation already holds more readmission signal than the head is using. This is fixable at the head/task-formulation level (a real architecture or training bug in the readmission hazard head, or a formulation issue specific to the 720h discharge-anchored landmark), independent of any backbone or bottleneck work.
Not yet done: `scripts/probe_ci_check.py` is uncommitted (VM-local only) and should be committed with a test before anyone else relies on it; the KDIGO AKI staging fix (3d7ecbb) and the tabicl_baseline offload work (351d613) landed on main after this run's commit (9285f68), so a rerun on current main would be needed before quoting these AKI numbers as current.

| subset_run_v8_taskset_v3_counting | cbm | MIMIC 30 shards (train), same 4 held-out shards as every other v8 row | 31a86b9 | Counting-hazard widening test (plan: crystalline-watching-phoenix). Mechanism under test: is a time-to-next-occurrence hazard target for a specific drug class, for a rate-stationary process, an estimator of the same rate a window COUNT estimates -- so does training 6 extra low-base-rate hazard targets (vasopressor, inotrope, antiarrhythmic, neuromuscular_blocker, corticosteroid, bicarbonate; new `code_regex` onset mechanism, regexes copied from `baseline_features.DRUG_CLASSES`) force a rate-like representation the same way the GBM's own count features are rate estimators? Exact `subset_run_v8_taskset_v3` recipe plus `auxiliary_event_names` widening `EventHazardHeads` from 6 to 12 event names (trivial width change, +270K params, no architecture change); these 6 are trained but never scored as alerts (`alerts.py` still iterates `alert_events_for`, not `hazard_events_for`) | done (2026-08-29) | 28,873 steps, 15269.4s (4h14m), best_val_loss=2.4268, no early stop, no crashes. **Regression guard (existing 6 curated alert tasks, hazard AUROC, baseline subset_run_v8_taskset_v3 -> new, same held-out rows)**: vasopressor_start 8h 0.896->0.911 (+0.015), 24h 0.833->0.862 (+0.029), 72h 0.755->0.802 (**+0.047**); icu_admission 8h 0.899->0.899 (0.000), 24h 0.843->0.845 (+0.002), 72h 0.773->0.778 (+0.005); death 8h 0.904->0.901 (-0.003), 24h 0.876->0.876 (0.000), 72h 0.850->0.848 (-0.002); sepsis3 8h 0.854->0.847 (-0.007), 24h 0.834->0.830 (-0.004), 72h 0.781->0.780 (-0.001); readmission_30d 168h 0.504->0.523 (+0.019), 720h 0.527->0.578 (**+0.051**). acute_kidney_injury moved too (8h 0.777->0.800, 24h 0.726->0.748, 72h 0.682->0.711) but is NOT a clean before/after -- the VM synced to current main between runs, which includes the KDIGO staging fix (3d7ecbb), so this delta is confounded and excluded from the reading below. Net: clean win on vasopressor_start (grows with horizon, plausibly the auxiliary "vasopressor" hazard target sharing signal directly) and readmission_30d (both horizons, the exact cell the CI-check row above flagged as head/formulation-limited); icu_admission/death/sepsis3 flat within noise (<=0.7pp); no regression anywhere. **Decision-rule test (`scripts/probe_counting_signal.py` rerun, 5 train/4 held-out shards, matching `probe_counting.log`'s baseline exactly): pooled mean R^2 pre-bottleneck 0.3059 (baseline 0.3111, -0.0052), post-bottleneck 0.3368 (baseline 0.3393, -0.0025) -- flat, marginally lower.** Per-target post-bottleneck R^2, baseline -> new (delta): vasopressor n_6h 0.2035->0.2158 (+0.0122), n_24h 0.3320->0.3343 (+0.0023), ever_visit 0.4185->0.4055 (-0.0130); inotrope n_6h 0.0071->0.0059 (-0.0012), n_24h 0.0148->0.0100 (-0.0049), ever_visit 0.0719->0.0414 (-0.0305); antiarrhythmic n_6h 0.0491->0.0489 (-0.0002), n_24h 0.0745->0.0696 (-0.0049), ever_visit 0.2797->0.2561 (-0.0236); neuromuscular_blocker n_6h 0.0241->0.0339 (+0.0098), n_24h 0.0643->0.0866 (+0.0224), ever_visit 0.2125->0.2056 (-0.0069); corticosteroid n_6h 0.0353->0.0366 (+0.0013), n_24h 0.0445->0.0421 (-0.0024), ever_visit 0.1462->0.1312 (-0.0150); bicarbonate n_6h 0.0552->0.0434 (-0.0118), n_24h 0.0787->0.0639 (-0.0148), ever_visit 0.1296->0.1174 (-0.0121). Of the 18 targeted sub-features, 5 improve (all small, +0.001 to +0.022), 1 is flat, 12 are flat-to-worse (down to -0.031); no target improves on ALL THREE of its own sub-features (n_6h/n_24h/ever_visit) at once -- vasopressor and neuromuscular_blocker each improve on the two count features but lose on ever_visit, the reverse of what the rate-recovery mechanism would predict if it were working (ever_visit, the coarsest/most rate-like target, should benefit most). **READING per the plan's pre-registered decision rule: this is a clean null on the mechanism, not a confirming result -- the objective-level fix (widening the hazard head with rate-estimating targets) does not measurably transfer to the counting probe's R^2 ceiling for the 6 targeted low-base-rate drug classes.** The architecture-capacity hypothesis (Mamba-2's diagonal-SSM state-tracking weakness limiting exact/near-exact counting regardless of what the training objective asks for) is correspondingly a bit stronger, though not proven by a single null. The vasopressor_start and readmission_30d alert-task gains are real and worth keeping (auxiliary_event_names costs nothing and helps two tasks) but are a DIFFERENT finding from the counting-recovery question this experiment was designed to answer -- do not conflate the two when reporting this row. |
| subset_run_v8_taskset_v3_resetprob0 | cbm | MIMIC 30 shards (train), same 4 held-out shards as every other v8 row | 9285f68 (training); eval tooling at 31a86b9 in the same worktree | **reset_prob=0.0 ablation.** Single-variable change from `subset_run_v8_taskset_v3`'s recipe: `TrainingConfig.reset_prob` 0.1->0.0, every other field byte-identical (verified via full config diff). Motivated by the counting-probe's worst-recovered targets (row above, `_counting`): does removing synthetic mid-visit history resets improve the model's ability to track low-base-rate drug-class counts, per the same rate/hazard-estimator logic tested there? Config diff and subject/event counts (29,970 subjects, 72,924,527 events, both arms) confirmed identical before drawing any conclusion. Coordinated live with peer session `odyssey-4d`, which was concurrently doing label/bug-fix work on the same VM and shared git history, to avoid resource or branch conflicts | done (2026-08-30) | **Training anomaly, mechanism jointly traced with peer session odyssey-4d**: 5,407 steps in 4,068s (67.8min) vs baseline's 28,873 steps in 16,583s (276min) for the SAME 2 epochs on the SAME data -- a ~4.1x wall-clock speedup and ~5.3x fewer gradient steps, not a smaller run. Mechanism: `reset_prob` fires at every visit-BOUNDARY token (`_LaneBuffer.append_patient`), and any event with no hospital-admission id is its own single-event "visit" (`sequences.py::_assign_visits`), so outpatient-dense stretches offer a reset almost every token. At reset_prob=0.1 this collapses average chunk fill to 15.4% real tokens (946,110,464 token-slots for 145,849,054 real tokens across the run); at reset_prob=0.0, fill is 82.3% (177,176,576 slots, same 145,849,054 real tokens). Same data, ~5.3x fewer/thicker optimizer steps -- "the arm saw less data" was the wrong first read (caught and corrected mid-investigation after the peer challenged it) and "same data, different optimization granularity" is the right one. Every registered run before this one used the 0.1 default, so this reads as a project-wide finding, not just an ablation result: most prior subset/full runs likely trained at ~15% chunk utilization without anyone deciding that deliberately. **Decision: `TrainingConfig.reset_prob` default changed 0.1->0.0 in this commit** -- see the alerts and probe evidence below for why, and the one caveat (icu_admission) that should stay on watch at the next full-scale run rather than block this change. **Counting probe rerun** (`scripts/probe_counting_signal.py`, 5 train/4 held-out shards, matching `probe_counting.log`'s baseline exactly): pooled mean R^2 pre-bottleneck 0.3585 (baseline 0.3111, +0.0474), post-bottleneck 0.3993 (baseline 0.3393, **+0.0600**) -- a broad improvement, but NOT concentrated on the 6 targeted low-base-rate drug classes the original hypothesis was about. Per-target post-bottleneck R^2, baseline -> new (delta): vasopressor n_6h 0.2035->0.1873 (-0.0162), n_24h 0.3320->0.3195 (-0.0125), ever_visit 0.4185->0.5241 (**+0.1056**); inotrope n_6h 0.0071->-0.0019 (-0.0090), n_24h 0.0148->0.0014 (-0.0134), ever_visit 0.0719->0.0500 (-0.0219); antiarrhythmic n_6h 0.0491->0.0335 (-0.0156), n_24h 0.0745->0.0400 (-0.0345), ever_visit 0.2797->0.3232 (+0.0435); neuromuscular_blocker n_6h 0.0241->0.0304 (+0.0063), n_24h 0.0643->0.0711 (+0.0068), ever_visit 0.2125->0.2595 (+0.0470); corticosteroid n_6h 0.0353->0.0281 (-0.0072), n_24h 0.0445->0.0353 (-0.0092), ever_visit 0.1462->0.1430 (-0.0032); bicarbonate n_6h 0.0552->0.0578 (+0.0026), n_24h 0.0787->0.0828 (+0.0041), ever_visit 0.1296->0.1728 (**+0.0432**). 8 of 18 targeted sub-features improve, 10 get worse; no drug class improves on all three of its own sub-features, and the improvements skew toward the coarsest `ever_visit` target (4 of the 6 classes' `ever_visit` improves, sometimes sharply) while the finer `n_6h`/`n_24h` counts are mixed-to-worse. **READING: the original "reset_prob erodes rare-class counting memory" hypothesis is NOT confirmed** -- this pattern (broad pooled gain, concentrated on coarse/high-level targets, inconsistent on fine-grained counts) looks like the general optimization-granularity effect from the training anomaly above (fewer, thicker gradient steps improving overall fit) rather than a targeted rare-event-memory mechanism. Family-level features (lab/visit/billing/diagnosis/procedure/medication counts, not drug-class-specific) improved broadly and consistently (+0.04 to +0.13 across all 18 family features, every single one positive) -- consistent with a general, not counting-specific, benefit. **Alerts regression guard, same held-out rows, hazard AUROC baseline -> new (delta), GBM control alongside to confirm stability**: landmark leg -- vasopressor_start 8h 0.896->0.887 (-0.009, GBM 0.934->0.933 flat), 24h 0.833->0.839 (+0.006, GBM 0.914->0.914 flat), 72h 0.755->0.775 (+0.020, GBM 0.883->0.886 flat); **icu_admission 8h 0.899->0.882 (-0.017, GBM 0.968->0.969 flat), 24h 0.843->0.818 (-0.025, GBM 0.954->0.953 flat), 72h 0.773->0.737 (-0.036, GBM 0.931->0.931 flat)** -- the one consistent, horizon-worsening regression in the whole table, and the GBM control's own stability (<=0.001 movement) rules out a comparator-noise explanation; death 8h 0.904->0.917 (+0.013), 24h 0.876->0.891 (+0.015), 72h 0.850->0.863 (+0.013) (GBM flat +/-0.003 throughout); sepsis3 8h 0.854->0.861 (+0.007), 24h 0.834->0.841 (+0.007), 72h 0.781->0.788 (+0.007) (GBM flat +/-0.003 throughout). acute_kidney_injury is CONFOUNDED and excluded from this reading: n at risk changed (95,471->79,389 at 8h, etc.), meaning the eval-tooling worktree picked up the KDIGO staging fix (3d7ecbb) between the two evals, same confound already documented on the `_counting` row above -- do not quote an AKI delta from this row. Readmission leg (clean, unconfounded: n/pos at risk match baseline exactly) -- **readmission_30d 168h 0.504->0.550 (+0.046, GBM 0.664->0.664 flat), 720h 0.527->0.611 (+0.084, GBM 0.723->0.723 flat)** -- the largest, cleanest win in the table, on the exact cell the CI-check row (above) flagged as head/formulation-limited rather than representation-limited. **Net verdict**: 4 of 6 tasks improve or are flat-to-mixed (death and sepsis3 consistently +0.7 to +1.5pp at every horizon; readmission +4.6 to +8.4pp, clean; vasopressor mixed, improving with horizon), 1 task regresses consistently (icu_admission, -1.7 to -3.6pp, worsening with horizon), 1 is confounded (AKI, no reading). Combined with the ~4-5x training-efficiency win, this supports adopting reset_prob=0.0 as the new default while flagging icu_admission for a repeat check at full scale (this is a 30-shard subset; GBM refit noise alone is 0.5-1.5pp typical, up to ~2.9pp on sparse cells, so icu_admission's regression is larger than that floor but has not been checked at scale). Decision (adopt 0.0 as `TrainingConfig`'s default) made explicitly by Amrit on this evidence, 2026-08-30. **Caveats for anyone using this row, flagged by peer session odyssey-4d before push:** (1) any run comparing against prior-generation (pre-2026-08-30) numbers must pin `reset_prob=0.1` explicitly, or the ~5.3x optimizer-step-granularity change confounds with whatever else changed (label/protocol fixes especially) -- `subset_run_v10_taskset_v3` (in flight on this VM at push time) pins 0.1 for exactly this reason, as the fixed-code like-for-like validation of the v8 recipe. (2) icu_admission is the natural seed-replicate candidate when the full-scale retrain matrix launches under the new default, since icu_admission cells already showed the largest cross-run variance in this registry's own noise-floor measurements (row 92-98 above). |
| full_run_v10 (R2) | cbm | MIMIC all 292 train shards, full held-out eval | cdbd4e7 (train + eval chain) | flagship post-fix MIMIC joint retrain: v10 recipe (task_set v3, 29 concepts, landmark v4, bin-edge v2, reset_prob 0.0, concept_supervision visit) | done (2026-08-31) | Training 40,750 steps, best_val 2.0556, ~7.2h. Eval chain all stages EXIT 0 (report 15:56 UTC). Set top-1 81.60 (prior-gen full_run_v8 80.4), top-1 37.25, top-5 69.94, xent 3.5560; 29-concept readout mean AUROC 0.9307 (0.775-0.999; prior-gen 0.857/15). Banded interventions (0.15, 76.1% coverage): none 37.25, truth -0.19, flip -0.05, random -0.12 (wrong sign SURVIVES on top-1); losses 3.5560/3.5717/3.5730/3.5723 -- prior-gen loss-ordering corroboration did NOT survive (substitutions within 0.0013 nats); displacement truth 0.4904/flip 0.5096. zero_known retains 6.6%, zero_unknown 98.4% and improves loss (3.494). Per-subject dump MISSING from this chain (pre-fix script); regenerated by the supplemental Guide Labs rerun for CIs. alerts.json + alerts_rows.parquet written (B1 gate). |
| eicu_full_v10 (R6) | eicu | eICU v2 all 134 train shards, full held-out eval | cdbd4e7 (train + eval chain) | flagship post-fix eICU joint retrain, same v10 recipe (26 concepts after source drops) | done (2026-08-31) | Training 35,500 steps, best_val 2.0148, ~4.9h. Eval chain all stages EXIT 0 (report 15:07 UTC). Set top-1 88.13 (prior-gen 87.5), top-1 54.01, top-5 87.59, xent 1.9750; 26-concept readout mean AUROC 0.8741 (0.6535-0.9655); time NLL 0.208. Banded interventions (0.15, 99.98% coverage): none 54.01, truth +0.19, flip -0.38, random -0.10; losses 1.9750/1.9985/2.0018/2.0003 (metric disagreement persists: truth helps top-1, hurts loss; but truth no longer worst-on-loss -- ordering now direction-consistent); displacement truth 0.4872/flip 0.5128 (prior-gen sign-flip on this arm did not survive). zero_known retains 48.5% (prior-gen 33.4), zero_unknown 100.7% and improves loss (1.9583). Per-subject dump missing, same recovery path. Paper joint cells swapped for both runs 2026-08-31 (main.tex header log has full provenance). |
| eicu_full_L_v10 (R9) | eicu | eICU v2 all 134 train shards, full held-out eval | b99be32 (eval chain; trained at 17d28fc) | L-series full scale: R6's exact config with `concept_global_pairs=True` as the ONLY change (verified by config diff: the two configs differ in that key and `output_dir`, nothing else). Tests whether the subset-L1 sign correction survives and becomes useful at full scale, per the Guide Labs/Steerling structural-cause argument (Sec 5.3). | training done (2026-08-31 18:04:57 UTC); eval chain in flight | **Training: 18,250 steps in 8,995.5s (~2.5h), early_stop=True at 15/15 evals without improvement, best_val_loss=2.2677, zero errors, checkpoint_final.pt written.** The capability cost of global pairs is now visible at full scale on eICU and it is LARGER than the subset L1 result suggested: against R6 (the same recipe with context-dependent pairs) this arm converges both EARLIER and WORSE -- 18,250 steps vs 35,500, best_val_loss 2.2677 vs 2.0148 (+0.253 nats). Early stopping fired on genuine plateau, not a crash. Read this as the training-side half of the L-series question: the lever/capability trade-off the paper argues for in Sec 5.3 shows up in the optimisation itself, before any intervention is scored. The intervention half (does truth-flip come out correctly signed, and does any mode beat `none`) waits on the eval chain. Eval chain launched 18:07:44 UTC with STREAM_BASELINE=1, LANES=64; it produces the Guide Labs modes, the attribution stage, and per-subject dumps. NOTE: the chain does NOT run scripts/intervention_cis.py -- that is a separate manual step on interventions_band15_per_subject.json. |
